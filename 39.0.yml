openapi: 3.0.2
info:
  title: VMware Cloud Director
  license:
    name: © 2025 Broadcom, Inc. All rights reserved.
  version: Version 39.0
paths:
  /admin/extension/service/aclrule/{id}:
    get:
      tags:
        - extension
      description: Retrieve (GET) ACL rule
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Delete (DELETE) ACL rule
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/rule/{ruleId}:
    get:
      tags:
        - extension
      description: Retrieves the specified VM Host affinity rule
      responses:
        2XX:
          description: representation of a VM affinity rule {@link VmAffinityRuleInfo}
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Deletes the specified VM Host affinity rule.
      responses:
        2XX:
          description: a task to monitor the deletion of the rule
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: ruleId
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/strandedItems:
    delete:
      tags:
        - extension
      description: Removes all stranded items.
      responses:
        2XX:
          description: Task created to remove all the stranded items
      security:
        - bearerAuth: []
  /admin/extension/service/apidefinition/{id}:
    get:
      tags:
        - extension
      description: Retrieve API definition.
      responses:
        2XX:
          description: '{@link AdminApiDefinitionType}'
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Deletes API definition.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/apifilter/{id}:
    get:
      tags:
        - extension
      description: Retrieves API filter.
      responses:
        2XX:
          description: '{@link ApiFilterType}'
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Deletes API filter.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/associations/{remoteSiteId}/{remoteOrgId}:
    get:
      tags:
        - admin
      description: Retrieves a specific member from the organization's associations as identified by identifier included in the request
      responses:
        2XX:
          description: the {@link OrgAssociationType} representing the particular organization association record. `null` if no member found.
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update an organization association
      responses:
        2XX:
          description: Reference to the task updating the org's association
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Removes the specified member from this organization's associations.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: remoteSiteId
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: remoteOrgId
        required: true
        schema:
          type: string
        style: simple
  /admin/catalog/{id}:
    get:
      tags:
        - admin
      description: Retrieve a catalog.
      responses:
        2XX:
          description: CatalogType
          content:
            application/vnd.vmware.admin.catalog+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_AdminCatalogType'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update the name, description storage profiles and distributed catalog settings of a catalog.
      responses:
        2XX:
          description: UserType
          content:
            application/vnd.vmware.admin.catalog+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_AdminCatalogType'
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Delete a catalog.
      parameters:
        - in: query
          name: force
          schema:
            $ref: '#/components/schemas/query-parameter_force'
          style: form
        - in: query
          name: recursive
          schema:
            $ref: '#/components/schemas/query-parameter_recursive'
          style: form
      responses:
        2XX:
          description: JAXBElement for API version 30.0 or greater, 204 no\_content for older API versions
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /catalogItem/{id}:
    get:
      tags:
        - user
      description: Retrieve a catalog item.
      responses:
        2XX:
          description: CatalogItemType
          content:
            application/vnd.vmware.vcloud.catalogItem+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_CatalogItemType'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Modify a catalog item.
      responses:
        2XX:
          description: CatalogItemType
          content:
            application/vnd.vmware.vcloud.catalogItem+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_CatalogItemType'
      security:
        - bearerAuth: []
    delete:
      tags:
        - user
      description: Delete a catalog item.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /catalogItem/{id}/metadata/{key}:
    get:
      tags:
        - user
      description: Retrieve the value of the specified key from catalog item metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Set the metadata value for the specified key to the value provided, overwriting any existing value.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - user
      description: Delete the specified key and its value from catalog item metadata.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /admin/catalog/{id}/metadata/{key}:
    get:
      tags:
        - admin
      description: Retrieve the value of the specified key from catalog metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Set the value for the specified metadata key to the value provided, overwriting any existing value.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Delete the specified key and its value from catalog metadata.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /disk/{id}:
    get:
      tags:
        - user
      description: Retrieve a disk.
      responses:
        2XX:
          description: DiskType
          content:
            application/vnd.vmware.vcloud.disk+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_DiskType'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: 'Update the name, description, iops, size and storage class of a disk. Beginning in 37.0, we have deprecated the ''iops'' field of {@link DiskType} and introduced a new type representing IOPS metrics: {@link IopsResourceType}. NOTE that while the original ''iops'' field still exists (which represents IOPS reservation), if both the old field and reservation in {@link IopsResourceType} are set, VCD will favor the IOPS reservation value within the new {@link IopsResourceType}.'
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    delete:
      tags:
        - user
      description: Delete a disk.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /disk/{id}/metadata/{key}:
    get:
      tags:
        - user
      description: Retrieve the value of the specified key from disk metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Set the value for the specified metadata key to the value provided, overwriting any existing value.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - user
      description: Delete the specified key and its value from disk metadata.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /admin/edgeGateway/{id}:
    get:
      tags:
        - admin
      description: Retrieve an edge gateway
      responses:
        2XX:
          description: GATEWAYM
          content:
            application/vnd.vmware.admin.edgeGateway+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_GatewayType'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update an edge gateway. Can only be used for Edge Gateways owned by an Organization vDC. API Version 35.0 is the last version this API is supported for NSX-T Edges.
      responses:
        2XX:
          description: TaskType
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Delete an edge gateway. Can only be used for Edge Gateways owned by an Organization vDC.
      parameters:
        - in: query
          name: force
          schema:
            $ref: '#/components/schemas/query-parameter_force'
          style: form
        - in: query
          name: recursive
          schema:
            $ref: '#/components/schemas/query-parameter_recursive'
          style: form
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/externalnet/{id}:
    get:
      tags:
        - extension
      description: Retrieve a system administrator view of an external network.
      responses:
        2XX:
          description: VMWExternalNetworkType
      deprecated: true
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update an external network.
      responses:
        2XX:
          description: VMWExternalNetworkType
      deprecated: true
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Delete an external network.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/file/{id}:
    get:
      tags:
        - extension
      description: Retrieve a file descriptor
      responses:
        2XX:
          description: '{@link AdminFileDescriptorType}'
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Deletes file descriptor.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/group/{id}:
    get:
      tags:
        - admin
      description: Retrieve a group.
      responses:
        2XX:
          description: GroupType
      deprecated: true
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update the name or description of a group.
      responses:
        2XX:
          description: GroupType
      deprecated: true
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Deletes a group.
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/settings/ldapSettings:
    get:
      tags:
        - extension
      description: Retrieve system LDAP settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update system LDAP settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Delete system LDAP settings
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
  /admin/extension/service/link/{id}:
    get:
      tags:
        - extension
      description: Retrieves service link.
      responses:
        2XX:
          description: '{@link ServiceConfigType}'
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Deletes service link.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /media/{id}:
    get:
      tags:
        - user
      description: Retrieve a media object.
      responses:
        2XX:
          description: MediaType
          content:
            application/vnd.vmware.vcloud.media+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_MediaType'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update the name and description of a media object.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    delete:
      tags:
        - user
      description: Delete a media object.
      parameters:
        - in: query
          name: force
          schema:
            $ref: '#/components/schemas/query-parameter_force'
          style: form
        - in: query
          name: recursive
          schema:
            $ref: '#/components/schemas/query-parameter_recursive'
          style: form
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /media/{id}/metadata/{key}:
    get:
      tags:
        - user
      description: Retrieve the value of the specified key from media object metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Set the value for the specified metadata key to the value provided, overwriting any existing value.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - user
      description: Delete the specified key and its value from media object metadata.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /admin/edgeGateway/{id}/metadata/{key}:
    get:
      tags:
        - admin
      description: Retrieve the value of the specified key from gateway metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Set the value for the specified metadata key to the value provided, overwriting any existing value.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Delete the specified key and its value from gateway metadata.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /admin/network/{id}:
    get:
      tags:
        - admin
      description: Retrieve an administrative view of a network.
      responses:
        2XX:
          description: ExternalNetworkType
          content:
            application/vnd.vmware.admin.vmwexternalnet+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_ExternalNetworkType'
      deprecated: true
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update an organization network.
      responses:
        2XX:
          description: TaskType This operation is asynchronous and the user should monitor the returned task status in order to check when it is completed.
      deprecated: true
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Delete a network.
      parameters:
        - in: query
          name: force
          schema:
            $ref: '#/components/schemas/query-parameter_force'
          style: form
        - in: query
          name: recursive
          schema:
            $ref: '#/components/schemas/query-parameter_recursive'
          style: form
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /network/{id}:
    get:
      tags:
        - user
      description: Retrieve an organization network.
      responses:
        2XX:
          description: OrgNetworkType
          content:
            application/vnd.vmware.vcloud.orgVdcNetwork+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_OrgVdcNetworkType'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/network/{id}/metadata/{key}:
    get:
      tags:
        - admin
      description: Retrieve the value of the specified key from network metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Set the value for the specified metadata key to the value provided, overwriting any existing value.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Delete the specified key and its value from network metadata.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/networkPool/{id}:
    get:
      tags:
        - extension
      description: Retrieve a network pool.
      responses:
        2XX:
          description: VMWNetworkPool
      deprecated: true
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update a network pool.
      responses:
        2XX:
          description: VMWNetworkPool
      deprecated: true
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Delete a network pool.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/settings/oauth:
    get:
      tags:
        - admin
      description: Retrieve organization OAuth settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update organization OAuth settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Remove OAuth settings from an organization.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/object/{id}:
    get:
      tags:
        - extension
      description: Retrieves a registered object extension.
      responses:
        2XX:
          description: representation of an object extension {@link ObjectExtensionType}
      deprecated: true
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Updates a registered object extension.
      responses:
        2XX:
          description: representation of an object extension {@link ObjectExtension}
      deprecated: true
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Deletes a registered object extension with specified ID.
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}:
    get:
      tags:
        - admin
      description: Retrieve an administrative view of an organization.
      responses:
        2XX:
          description: AdminOrgType
          content:
            application/vnd.vmware.admin.organization+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_AdminOrgType'
      deprecated: true
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update an organization.
      responses:
        2XX:
          description: AdminOrgType
      deprecated: true
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: |-
        Delete an organization.

         Before you can delete an organization, you must disable it and then delete or change ownership of all objects that the organization's users own.
      parameters:
        - in: query
          name: force
          description: pass force=true as query parameter along with recursive=true to remove an organization or VDC and any objects it contains, regardless of their state.
          schema:
            $ref: '#/components/schemas/query-parameter_force'
          style: form
        - in: query
          name: recursive
          description: pass recursive=true as query parameter to remove an organization or VDC and any objects it contains that are in a state that normally allows removal.
          schema:
            $ref: '#/components/schemas/query-parameter_recursive'
          style: form
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/metadata/{key}:
    get:
      tags:
        - admin
      description: Retrieve the value of the specified key from organization metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Set the value for the specified metadata key to the value provided, overwriting any existing value.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Delete the specified key and its value from organization metadata.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/providervdc/{id}:
    get:
      tags:
        - extension
      description: Retrieve a system administrator view of a provider vDC.
      responses:
        2XX:
          description: VMWProviderVdc
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update a provider vDC.
      responses:
        2XX:
          description: VMWProviderVdc
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: |-
        Delete a provider vDC.

         Before you can delete a provider vDC, you must disable it.
      responses:
        2XX:
          description: |-
            Task

             This operation is asynchronous and returns a Task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/providervdc/{id}/metadata/{key}:
    get:
      tags:
        - admin
      description: Retrieve the value of the specified key from provider vDC metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Set the value for the specified metadata key to the value provided, overwriting any existing value.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Delete the specified key and its value from provider vDC metadata.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /admin/pvdcStorageProfile/{id}/metadata/{key}:
    get:
      tags:
        - admin
      description: Retrieve the value of the specified key from Provider VDC Storage Profile metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Set the value for the specified metadata key to the value provided, overwriting any existing value.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Delete the specified key and its value from Provider VDC Storage Profile metadata.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/resourceclass/{id}:
    get:
      tags:
        - extension
      description: Retrieve (GET) resource class info
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Deletes (DELETE) resource class
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/resourceclassaction/{id}:
    get:
      tags:
        - extension
      description: Retrieve (GET) resource class action info
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Deletes (DELETE) resource class action
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/right/{id}:
    get:
      tags:
        - admin
      description: Retrieves a right.
      responses:
        2XX:
          description: RightType
          content:
            application/vnd.vmware.admin.right+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_RightType'
      deprecated: true
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Updates a right.
      responses:
        2XX:
          description: RightType
          content:
            application/vnd.vmware.admin.right+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_RightType'
      deprecated: true
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Deletes a right.
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{orgId}/right/{id}:
    get:
      tags:
        - admin
      description: Retrieves right in the context of an organization
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Unlink a right from an organization
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: orgId
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{orgId}/rights:
    get:
      tags:
        - admin
      description: Retrieve rights for an organization
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Modify rights for an organization
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Add rights to an organization
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Remove all rights from an organization
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: orgId
        required: true
        schema:
          type: string
        style: simple
  /admin/role/{id}:
    get:
      tags:
        - admin
      description: Retrieve a role.
      responses:
        2XX:
          description: RoleType
          content:
            application/vnd.vmware.admin.role+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_RoleType'
      deprecated: true
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: |-
        Update a role.

         This request changes the name or description of the role, and adds or removes rights.
      responses:
        2XX:
          description: RoleType
      deprecated: true
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Delete a role.
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/vdcs/extension/{selectorExtensionId}:
    get:
      tags:
        - admin
      description: |-
        Retrieves a Selector Extension.

         The association can be the default policy, or an overridden instance of the default policy.
      responses:
        2XX:
          description: representation of a Selector Extension {@link SelectorExtensionType}
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: |-
        Updates a Selector Extension.

         The association can be the default policy, or an overridden instance of the default policy.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: |-
        Deletes a Selector Extension.

         The association can be the default policy, or an overridden instance of the default policy.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: selectorExtensionId
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/{id}:
    get:
      tags:
        - extension
      description: Gives information about a concrete extension service.
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Updates already registered service.
      responses:
        2XX:
          description: '{@link AdminServiceType}'
      deprecated: true
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Deletes registered service.
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/serviceresource/{id}:
    get:
      tags:
        - extension
      description: Retrieve (GET) service resource
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Delete (DELETE) service resource
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /shadowVm/{id}:
    get:
      tags:
        - user
      description: Retrieve a shadow VM.
      responses:
        2XX:
          description: '{@link VAppTemplateType}'
      security:
        - bearerAuth: []
    delete:
      tags:
        - user
      description: |-
        Deletes shadow VM.

         This API for deletion of shadow VMs has changed URL signature to better reflect the API object model.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /site/associations/{id}:
    get:
      tags:
        - user
      description: Get the {@link SiteAssociationType} for the specified site id. GET /api/site/associations/{siteId}
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Adds an association for the specified site. PUT /api/site/associations/{siteId}
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - user
      description: Deletes the association for the specified site. DELETE /api/site/associations/{siteId}
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/strandedItem/{id}:
    get:
      tags:
        - extension
      description: Gets the representation of a strandedItem.
      responses:
        2XX:
          description: Returns the requested stranded item.
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Removes stranded item.
      responses:
        2XX:
          description: Task created to remove the stranded item
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vdcTemplate/{id}:
    get:
      tags:
        - extension
      description: Retrieve a vDC template.
      responses:
        2XX:
          description: vdcTemplate the VDC Template whose id was given as input.
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update a vDC template.
      responses:
        2XX:
          description: vdcTemplate the new contents of the VDC Template.
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Delete a vDC template.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/vdcTemplate/{id}:
    get:
      tags:
        - admin
      description: Retrieve a vDC template.
      responses:
        2XX:
          description: vdcTemplate the VDC Template whose id was given as input.
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update a vDC template.
      responses:
        2XX:
          description: vdcTemplate the new contents of the VDC Template.
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Delete a vDC template.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/nsxtManagers/{id}:
    get:
      tags:
        - extension
      description: Retrieve the representation of a registered NSX-T manager.
      responses:
        2XX:
          description: NsxTManagerType
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update an existing NSX-T Manager.
      responses:
        2XX:
          description: updated NSX-T Manager
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Delete an existing NSX-T Manager.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /nsxtManagers/{id}:
    get:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/user/{id}:
    get:
      tags:
        - admin
      description: Retrieve a user.
      responses:
        2XX:
          description: UserType
          content:
            application/vnd.vmware.admin.user+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_UserType'
      deprecated: true
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: |-
        Update a user.

         Use this request to update any property of a local user, or to update the name or description of an imported user. A locked user cannot be updated.
      responses:
        2XX:
          description: UserType
      deprecated: true
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Delete a user.
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}:
    get:
      tags:
        - user
      description: Retrieve a vApp or VM.
      responses:
        2XX:
          description: VAppType
          content:
            application/vnd.vmware.vcloud.vApp+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_VAppType'
            application/vnd.vmware.vcloud.vm+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_VmType'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Modify the name or description of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    delete:
      tags:
        - user
      description: Delete a vApp or VM.
      parameters:
        - in: query
          name: force
          schema:
            $ref: '#/components/schemas/query-parameter_force'
          style: form
        - in: query
          name: recursive
          schema:
            $ref: '#/components/schemas/query-parameter_recursive'
          style: form
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/metadata/{key}:
    get:
      tags:
        - user
      description: Retrieve the value of the specified key from vApp or VM metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Set the value for the specified metadata key to the value provided, overwriting any existing value.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - user
      description: Delete the specified key and its value from vApp or VM metadata.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}:
    get:
      tags:
        - user
      description: Retrieve a vApp template.
      responses:
        2XX:
          description: VAppTemplateType
          content:
            application/vnd.vmware.vcloud.vAppTemplate+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_VAppTemplateType'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update the name, description and gold master of a vApp template. Also updates the compute policies of a template's VMs if given.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    delete:
      tags:
        - user
      description: Delete a vApp template.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/metadata/{key}:
    get:
      tags:
        - user
      description: Retrieve the value of the specified key from vApp template or VM metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Set the value for the specified metadata key to the value provided, overwriting any existing value.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - user
      description: Delete the specified key and its value from vApp template or VM metadata.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /admin/vdc/{id}:
    get:
      tags:
        - admin
      description: Retrieve the admin view of an organization vDC.
      responses:
        2XX:
          description: AdminVdcType
          content:
            application/vnd.vmware.admin.vdc+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_AdminVdcType'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Modify an organization vDC.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: |-
        Delete an organization vDC.

         You must disable the virtual datacenter before you can delete it.
      parameters:
        - in: query
          name: force
          schema:
            $ref: '#/components/schemas/query-parameter_force'
          style: form
        - in: query
          name: recursive
          schema:
            $ref: '#/components/schemas/query-parameter_recursive'
          style: form
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}:
    get:
      tags:
        - user
      description: Retrieve a vDC.
      responses:
        2XX:
          description: VdcType
          content:
            application/vnd.vmware.vcloud.vdc+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_VdcType'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: |-
        Modify the name and/or description of an organization vDC.

         This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      responses:
        2XX:
          description: TaskType
      security:
        - bearerAuth: []
    delete:
      tags:
        - user
      description: |-
        Delete an organization vDC.

         You must disable the virtual datacenter before you can delete it, unless {@code RestConstants.DeleteParameters.FORCE} is asserted
      parameters:
        - in: query
          name: force
          schema:
            $ref: '#/components/schemas/query-parameter_force'
          style: form
        - in: query
          name: recursive
          schema:
            $ref: '#/components/schemas/query-parameter_recursive'
          style: form
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/vdc/{id}/metadata/{key}:
    get:
      tags:
        - admin
      description: Retrieve the value of the specified key from virtual datacenter metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Set the value for the specified metadata key to the value provided, overwriting any existing value.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Delete the specified key and its value from virtual datacenter metadata.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /admin/vdcStorageProfile/{id}/metadata/{key}:
    get:
      tags:
        - admin
      description: Retrieve the value of the specified key from vDC storage profile metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Set the value for the specified metadata key to the value provided, overwriting any existing value.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - admin
      description: Delete the specified key and its value from vDC storage profile metadata.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}/vmAffinityRules/{ruleId}:
    get:
      tags:
        - user
      description: Retrieves the specified VM affinity rule
      responses:
        2XX:
          description: representation of a VM affinity rule {@link VmAffinityRuleInfo}
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Updates the specified VM affinity/anti-affinity rule. This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      responses:
        2XX:
          description: representation of a Task {@link TaskSpec}
      security:
        - bearerAuth: []
    delete:
      tags:
        - user
      description: Deletes the specified VM affinity/anti-affinity rule
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: ruleId
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vmGroup/{groupId}:
    get:
      tags:
        - extension
      description: Retrieves the vm group by Id
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    delete:
      tags:
        - extension
      description: Delete the specified VM group
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: groupId
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/resourceclassaction/{id}/aclrules:
    get:
      tags:
        - extension
      description: List registered resource class ACL rule for extension service.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.query.records+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_QueryResultRecordsType'
      security:
        - bearerAuth: []
    post:
      tags:
        - extension
      description: Registers resource class ACL rule for extension service.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/blockingTasks:
    get:
      tags:
        - extension
      description: Retrieve a list of active or recently completed blocking task requests.
      responses:
        2XX:
          description: '{@link ReferencesType}'
      security:
        - bearerAuth: []
  /task/operations:
    get:
      tags:
        - user
      description: Returns all task operations with their categories.
      responses:
        2XX:
          description: TaskOperationListType
      security:
        - bearerAuth: []
  /admin/network/{id}/allocatedAddresses:
    get:
      tags:
        - admin
      description: Retrieve the list of IP addresses allocated to the network. Starting from API Version 35.0, this API is deprecated when using an Org vDC Network. Refer to OpenAPI.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /network/{id}/allocatedAddresses:
    get:
      tags:
        - user
      description: Retrieve a list of IP addresses allocated to the network.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/settings/amqp:
    get:
      tags:
        - extension
      description: Retrieve system AMQP settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      deprecated: true
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update system AMQP settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      deprecated: true
      security:
        - bearerAuth: []
  /apidefinition/{id}:
    get:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: '{@link ApiDefinitionType}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/{id}/apidefinitions:
    get:
      tags:
        - extension
      description: Returns the API definitions registered by this service.
      responses:
        2XX:
          description: List of {@link QueryResultApiDefinitionRecordType}
          content:
            application/vnd.vmware.vcloud.query.records+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_QueryResultRecordsType'
      security:
        - bearerAuth: []
    post:
      tags:
        - extension
      description: Creates API definition.
      responses:
        2XX:
          description: '{@link AdminApiDefinitionType}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /apidefinitions:
    get:
      tags:
        - user
      description: Return all API definitions.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.query.records+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_QueryResultRecordsType'
      security:
        - bearerAuth: []
  /files:
    get:
      tags:
        - user
      description: Return all API definition file descriptors.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.query.records+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_QueryResultRecordsType'
      security:
        - bearerAuth: []
  /service/{id}/apidefinitions:
    get:
      tags:
        - user
      description: Return all Extension Service API Definitions.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.query.records+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_QueryResultRecordsType'
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /extensibility:
    get:
      tags:
        - user
      description: Retrieve a list of links to API extensibility operations and entities.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.apiextensibility+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_ApiExtensibilityType'
      security:
        - bearerAuth: []
  /admin/extension/service/{id}/apifilters:
    get:
      tags:
        - extension
      description: Returns the API filters registered by this service.
      responses:
        2XX:
          description: List of {@link QueryResultApiFilterRecordType}
      security:
        - bearerAuth: []
    post:
      tags:
        - extension
      description: Creates API filter.
      responses:
        2XX:
          description: '{@link ApiFilterType}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/associations/localAssociationData:
    get:
      tags:
        - admin
      description: Gets the associaiton data for a given local org. This can be pasted into the body of a request to add an associated org on a remote site
      responses:
        2XX:
          description: '{@link OrgAssociationType} containing the local org''s info'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/associations:
    get:
      tags:
        - admin
      description: Retrieves the complete list of Organizations associated with this organization
      responses:
        2XX:
          description: non-null, possibly empty {@link List} of {@link OrgAssociationsType} representing this organization's associations
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: |-
        Replaces the organization's current list of association members with the specified list of members

         If the list is empty, all existing members will be deleted.
      responses:
        2XX:
          description: Reference to the task updating the org's associations
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Adds a new member to this organization's associations
      responses:
        2XX:
          description: Reference to the task performing the association
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vimServer/{id}/hostReferences:
    get:
      tags:
        - extension
      description: Retrieve the list of ESX/ESXi hosts in the cloud.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vimServer/{id}/networks:
    get:
      tags:
        - extension
      description: |-
        Retrieve a list of available vCenter networks and portgroups.

         Networks and portgroups that are already in use by a vCloud Director network are not listed.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/providervdc/{id}/availableStorageProfiles:
    get:
      tags:
        - extension
      description: Retrieve a list of storage classes that can be added to the specified provider vDC. Storage classes that have already been added to the provider vDC are not included in the list.
      responses:
        2XX:
          description: '{@link VMWStorageProfilesType}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vimServer/{id}/resourcePoolList:
    get:
      tags:
        - extension
      description: |-
        Retrieve a list of unused resource pools from a vCenter server.

         Resource pools that are already in use by a provider vDC are not listed.
      responses:
        2XX:
          description: ResourcePoolListType
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/blockingTask/{id}:
    get:
      tags:
        - extension
      description: Retrieve the representation of a blocking task.
      responses:
        2XX:
          description: BlockingTask
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/settings/blockingTask/operations:
    get:
      tags:
        - extension
      description: Retrieve a list of operations that are configured as blocking tasks.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update the list of operations that are configured as blocking tasks.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
  /admin/extension/settings/blockingTask:
    get:
      tags:
        - extension
      description: Retrieve system blocking task settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update system blocking task settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
  /vApp/{id}/bootOptions:
    get:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/settings/branding:
    get:
      tags:
        - extension
      description: |-
        Retrieve system branding settings.

         You can use system branding settings to customize the vCloud Director Web console and some of the links that appear on the vCloud Director Home login screen.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: |-
        Update system branding settings.

         You can use system branding settings to customize the vCloud Director Web console and some of the links that appear on the vCloud Director Home login screen.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
  /catalog/{id}:
    get:
      tags:
        - user
      description: Retrieve a catalog.
      responses:
        2XX:
          description: CatalogType
          content:
            application/vnd.vmware.vcloud.catalog+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_CatalogType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /catalogItem/{id}/metadata:
    get:
      tags:
        - user
      description: Retrieve all metadata associated with a catalog item.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    post:
      tags:
        - user
      description: Merge the metadata provided in the request with existing catalog item metadata.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/catalog/{id}/metadata:
    get:
      tags:
        - admin
      description: Retrieve metadata associated with the catalog.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Merge the metadata provided in the request with existing metadata.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /catalog/{id}/metadata:
    get:
      tags:
        - user
      description: Retrieve all catalog metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/catalog/{id}/metadata/{domain}/{key}:
    get:
      tags:
        - admin
      description: Retrieve the value of the specified key in the specified domain from catalog metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: domain
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /admin/catalog/{id}/owner:
    get:
      tags:
        - admin
      description: Retrieve the owner of a catalog.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: |-
        Update the owner of a catalog.

         The request body specifies the new owner
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /catalogs/query:
    get:
      tags:
        - user
      description: Retrieves a list of Catalogs by using REST API general QueryHandler; If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - references or records. Default format is references.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      security:
        - bearerAuth: []
  /vApp/{id}/checkpostcustomizationscript:
    get:
      tags:
        - user
      description: Retrieve the flag value check\_post\_guest\_customization. This flag decides if property collector would wait for post guest customization script to complete
      responses:
        2XX:
          description: VmCheckPGCType
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Modify the PostGC Status check of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation sets the flag check\_post\_guest\_customization
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vdcTemplate/{id}/controlAccess:
    get:
      tags:
        - extension
      description: Retrieve access control information for a VDC template.
      responses:
        2XX:
          description: the ACL for that VDC Template.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /org/{id}/catalog/{catalogId}/controlAccess:
    get:
      tags:
        - user
      description: Retrieve access control information for a catalog.
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: catalogId
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/controlAccess:
    get:
      tags:
        - user
      description: Retrieve access control information for a vApp.
      responses:
        2XX:
          description: ControlAccessParamsType
          content:
            application/vnd.vmware.vcloud.controlAccess+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_ControlAccessParamsType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}/controlAccess:
    get:
      tags:
        - user
      description: Retrieve access control information for a VDC.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/vdcTemplate/{id}/controlAccess:
    get:
      tags:
        - admin
      description: Retrieve access control information for a VDC template.
      responses:
        2XX:
          description: the ACL for that VDC Template.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/settings/cpom:
    get:
      tags:
        - extension
      description: Retrieve the system-level settings for CPoM.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update CPoM settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
  /vApp/{id}/virtualHardwareSection/cpu:
    get:
      tags:
        - user
      description: Retrieve the RASD item that specifies CPU properties of a VM.
      responses:
        2XX:
          description: ovf:RASD\_Type
          content:
            application/vnd.vmware.vcloud.rasdItem+json;version=39.0:
              schema:
                $ref: '#/components/schemas/ovf_RASD_Type'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update the RASD item that specifies CPU properties of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/virtualHardwareSection/cpu:
    get:
      tags:
        - user
      description: Retrieve the RASD item that specifies CPU properties of a VM.
      responses:
        2XX:
          description: ovf:RASD\_Type
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update the RASD item that specifies CPU properties of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /session:
    get:
      tags:
        - user
      description: |-
        Retrieve a representation of the current session.

         A Session object contains one or more URLs from which you can begin browsing, as well as links to the query service and entity resolver. The list of URLs in the Session object is based on the role and privileges of the authenticated user.

         A Session object expires after a configurable interval of client inactivity. To change the length of this client inactivity timeout, a system administrator can change the value of SessionTimeoutMinutes in the system's GeneralSettings
      responses:
        2XX:
          description: The current session
          content:
            application/vnd.vmware.vcloud.session+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_SessionType'
      deprecated: true
      security:
        - bearerAuth: []
  /admin/extension/datastore/{id}:
    get:
      tags:
        - extension
      description: Retrieve a datastore.
      responses:
        2XX:
          description: Datastore
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update datastore properties, including name, description, and low disk space thresholds.
      responses:
        2XX:
          description: Datastore
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/datastores:
    get:
      tags:
        - extension
      description: Retrieve a list of references to datastores in the system.
      responses:
        2XX:
          description: DatastoreReferences
      security:
        - bearerAuth: []
  /admin/extension/datastores/query:
    get:
      tags:
        - extension
      description: Get a list of all datastores by using REST API General QueryHandler. This is read only list of datastores. If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - referenceView or simpleView. Default format is referenceView.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      security:
        - bearerAuth: []
  /admin/extension/providervdc/{id}/discoverResourcePools:
    get:
      tags:
        - extension
      description: Get direct children resource pools of Provider VDC that are valid candidates for creating a new VDC
      responses:
        2XX:
          description: '{@link VMWDiscoveredResourcePoolsType}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/providervdc/{id}/discoverResourcePools/{parent_rp_moref}:
    get:
      tags:
        - extension
      description: Get direct children resource pools of {parent\_rp\_moref} nested under the Provider VDC which are valid candidates for creating a new VDC.
      responses:
        2XX:
          description: '{@link VMWDiscoveredResourcePoolsType}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: parent_rp_moref
        required: true
        schema:
          type: string
        style: simple
  /disk/{id}/metadata:
    get:
      tags:
        - user
      description: Retrieve metadata associated with the disk.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    post:
      tags:
        - user
      description: Merge the metadata provided in the request with existing metadata.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /disk/{id}/owner:
    get:
      tags:
        - user
      description: Retrieve the owner of a disk.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Change the owner of a disk.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /disks/query:
    get:
      tags:
        - user
      description: Retrieves a disk list by using REST API general QueryHandler; If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - references or records. Default format is references.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      security:
        - bearerAuth: []
  /vApp/{id}/virtualHardwareSection/disks:
    get:
      tags:
        - user
      description: Retrieve all RASD items that specify hard disk and hard disk controller properties of a VM.
      responses:
        2XX:
          description: RasdItemsListType
          content:
            application/vnd.vmware.vcloud.rasdItemsList+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_RasdItemsListType'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update all RASD items that specify hard disk and hard disk controller properties of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/virtualHardwareSection/disks:
    get:
      tags:
        - user
      description: Retrieve all RASD items that specify hard disk and hard disk controller properties of a VM.
      responses:
        2XX:
          description: RasdItemsListType
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update all RASD items that specify hard disk and hard disk controller properties of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/settings/email:
    get:
      tags:
        - extension
      description: Retrieve system email settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update system email settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
  /entity/{id}:
    get:
      tags:
        - user
      description: Redirects to the URL of an entity with the given VCD ID.
      responses:
        2XX:
          description: Response
          content:
            application/vnd.vmware.vcloud.entity+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_EntityType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/event/{id}:
    get:
      tags:
        - admin
      description: Returns event with specified ID.
      responses:
        2XX:
          description: event details
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /query:
    get:
      tags:
        - user
      description: REST API General queries handler
      parameters:
        - in: query
          name: type
          schema:
            $ref: '#/components/schemas/query-parameter_type'
          style: form
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns result of the query executed with provided filter in xml format.
          content:
            application/vnd.vmware.vcloud.query.records+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_QueryResultRecordsType'
            application/vnd.vmware.vcloud.query.idrecords+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_QueryResultRecordsType'
            application/vnd.vmware.vcloud.query.references+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_ReferencesType'
            application/vnd.vmware.vcloud.query.queryList+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_QueryListType'
      security:
        - bearerAuth: []
  /admin/extension:
    get:
      tags:
        - extension
      description: |-
        Retrieve a list of extension objects and operations.

         The VMWExtension element provides access to a cloud-wide namespace of vSphere platform objects that are registered for use by the system, and links that allow you to add vSphere servers and related resources such as networks and resource pools to the cloud. It also provides access to system level configuration settings for Kerberos and LDAP services.
      responses:
        2XX:
          description: VMWExtensionType
      security:
        - bearerAuth: []
  /admin/extension/service:
    get:
      tags:
        - extension
      description: Retrieves a list of extension service operations.
      responses:
        2XX:
          description: Representation of extension service operations.
      deprecated: true
      security:
        - bearerAuth: []
    post:
      tags:
        - extension
      description: Creates service.
      responses:
        2XX:
          description: the newly created {@link AdminServiceType}
      deprecated: true
      security:
        - bearerAuth: []
  /admin/extension/externalNetworkReferences:
    get:
      tags:
        - extension
      description: Retrieve a list of references to external networks in the system.
      responses:
        2XX:
          description: VMWExternalNetworkReferences
      deprecated: true
      security:
        - bearerAuth: []
  /admin/extension/externalNetworkReferences/query:
    get:
      tags:
        - extension
      description: Get list of external networks by using REST API general QueryHandler.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      deprecated: true
      security:
        - bearerAuth: []
  /admin/org/{id}/settings/federation:
    get:
      tags:
        - admin
      description: Retrieve other organization settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update other organization settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/apidefinition/{id}/files:
    get:
      tags:
        - extension
      description: Returns all file descriptors for the API definition.
      responses:
        2XX:
          description: List of {@link QueryResultFileDescriptorRecordType}
          content:
            application/vnd.vmware.vcloud.query.records+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_QueryResultRecordsType'
      security:
        - bearerAuth: []
    post:
      tags:
        - extension
      description: Creates file descriptor.
      responses:
        2XX:
          description: '{@link AdminFileDescriptorType}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /apidefinition/{id}/files:
    get:
      tags:
        - user
      description: Returns all file descriptors for the API definition.
      responses:
        2XX:
          description: List of {@link QueryResultFileDescriptorRecordType}
          content:
            application/vnd.vmware.vcloud.query.records+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_QueryResultRecordsType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/settings/general:
    get:
      tags:
        - extension
      description: |-
        Retrieve other system settings.

         These settings include catalog publishing rights, VM quotas, and related system-wide configuration defaults.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: |-
        Update other system settings.

         These settings include catalog publishing rights, VM quotas, and related system-wide configuration defaults.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
  /admin/org/{id}/settings/general:
    get:
      tags:
        - admin
      description: Retrieve other organization settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update other organization settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/groups/query:
    get:
      tags:
        - admin
      description: Retrieves a list of groups for organization the org admin belongs to by using REST API general QueryHandler; If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - references or records. Default format is references.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      deprecated: true
      security:
        - bearerAuth: []
  /vApp/{id}/guestcustomizationstatus:
    get:
      tags:
        - user
      description: 'Retrieve status of guest customization for that VM Following are the possible status 0 - PENDING\_GC : Guest Customization is either not started or in progress. This is set whenever a VM need customization. 1 - PENDING\_REBOOT : GC is successful, but reboot is pending. This is set when vCloud receives "guestinfo.gc.status" as successful 2 - PENDING\_POST\_GC : Reboot has happened, waiting for post gc script to complete. This is set when tools running status comes as "Running" based on conditions 1 & 2 3 - COMPLETE : GC is complete.'
      responses:
        2XX:
          description: VmCheckPGCType
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/guestCustomizationSection:
    get:
      tags:
        - user
      description: Retrieves the guest customization section of a VM.
      responses:
        2XX:
          description: GuestCustomizationSectionType
          content:
            application/vnd.vmware.vcloud.guestCustomizationSection+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_GuestCustomizationSectionType'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Updates the guest customization section of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/guestCustomizationSection:
    get:
      tags:
        - user
      description: Retrieves the guest customization section of a VM.
      responses:
        2XX:
          description: GuestCustomizationSectionType
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Updates the guest customization section of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/providervdc/{pvdcId}/hwv/{hwVersionId}:
    get:
      tags:
        - admin
      description: Retrieve a Virtual Hardware Version.
      responses:
        2XX:
          description: VirtualHardwareVersionType
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: pvdcId
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: hwVersionId
        required: true
        schema:
          type: string
        style: simple
  /vdc/{vdcId}/hwv/{hwVersionId}:
    get:
      tags:
        - user
      description: Retrieve a Virtual Hardware Version.
      responses:
        2XX:
          description: VirtualHardwareVersionType
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: vdcId
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: hwVersionId
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/host/{id}:
    get:
      tags:
        - extension
      description: Retrieve a host.
      responses:
        2XX:
          description: HostType
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/hostGroup/{groupId}:
    get:
      tags:
        - extension
      description: Retrieves the host group by Id
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: groupId
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/resourcePool/{rpId}/hostGroups:
    get:
      tags:
        - extension
      description: List all HostGroups within this resource pool.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: rpId
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/hostReferences:
    get:
      tags:
        - extension
      description: Retrieve a list of references to ESX/ESXi hosts attached to the system.
      responses:
        2XX:
          description: VMWHostReferences
      security:
        - bearerAuth: []
  /admin/extension/hostReferences/query:
    get:
      tags:
        - extension
      description: Get list of hosts by using REST API general QueryHAndler; This is read only list of host references and is not bound to specific states. If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - referenceView or simpleView. Default format is referenceView.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      security:
        - bearerAuth: []
  /vApp/{id}/leaseSettingsSection:
    get:
      tags:
        - user
      description: Retrieves the lease settings section of a vApp or vApp template
      responses:
        2XX:
          description: LeaseSettingsSectionType
          content:
            application/vnd.vmware.vcloud.leaseSettingsSection+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_LeaseSettingsSectionType'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Updates the lease settings section of a vApp or vApp template.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/leaseSettingsSection:
    get:
      tags:
        - user
      description: Retrieves the lease settings section of a vApp or vApp template
      responses:
        2XX:
          description: LeaseSettingsSectionType
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Updates the lease settings section of a vApp or vApp template.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/settings/catalog:
    get:
      tags:
        - extension
      description: Retrieve vCloud Director catalog setting details.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update catalog settings details.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
  /admin/extension/settings/license:
    get:
      tags:
        - extension
      description: Retrieve vCloud Director software license details.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update vCloud Director software license details.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
  /admin/extension/service/{id}/links:
    get:
      tags:
        - extension
      description: Returns the links registered by this service.
      responses:
        2XX:
          description: List of {@link QueryResultServiceLinkRecordType}
          content:
            application/vnd.vmware.vcloud.query.records+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_QueryResultRecordsType'
      security:
        - bearerAuth: []
    post:
      tags:
        - extension
      description: Create service link.
      responses:
        2XX:
          description: '{@link AdminServiceLinkType}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /site:
    get:
      tags:
        - user
      description: Get the {@link SiteType} that represents this local site. GET /api/site
      responses:
        2XX:
          description: The information about this site.
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Sets the local site to match what is specified in the {@link SiteType}. It can contain a list of associated sites in addition to info specific to the local site PUT /api/site
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
  /vApp/{id}/virtualHardwareSection/media:
    get:
      tags:
        - user
      description: Retrieve all RASD items that specify CD-ROM, DVD, and floppy disk device and controller properties of a VM.
      responses:
        2XX:
          description: RasdItemsListType
          content:
            application/vnd.vmware.vcloud.rasdItemsList+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_RasdItemsListType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/virtualHardwareSection/media:
    get:
      tags:
        - user
      description: Retrieve all RASD items that specify CD-ROM, DVD, and floppy disk device and controller properties of a VM.
      responses:
        2XX:
          description: RasdItemsListType
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /media/{id}/metadata:
    get:
      tags:
        - user
      description: Retrieve metadata associated with a media object.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    post:
      tags:
        - user
      description: Merge the metadata provided in the request with existing metadata.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /media/{id}/owner:
    get:
      tags:
        - user
      description: Retrieve the owner of a media object.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /mediaList/query:
    get:
      tags:
        - user
      description: Retrieves a media list by using REST API general QueryHandler; If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - references or records. Default format is references.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      security:
        - bearerAuth: []
  /vApp/{id}/virtualHardwareSection/memory:
    get:
      tags:
        - user
      description: Retrieve the RASD item that specifies memory properties of a VM.
      responses:
        2XX:
          description: ovf:RASD\_Type
          content:
            application/vnd.vmware.vcloud.rasdItem+json;version=39.0:
              schema:
                $ref: '#/components/schemas/ovf_RASD_Type'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update the RASD item that specifies memory properties of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/virtualHardwareSection/memory:
    get:
      tags:
        - user
      description: Retrieve the RASD item that specifies memory properties of a VM.
      responses:
        2XX:
          description: ovf:RASD\_Type
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update the RASD item that specifies memory properties of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/edgeGateway/{id}/metadata:
    get:
      tags:
        - admin
      description: Retrieve metadata associated with the gateway.
      responses:
        2XX:
          description: '{@link JAXBElement\< MetadataType \>}'
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Sets all the metadata for the specified gateway.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vmGroup/name/{groupName}:
    get:
      tags:
        - extension
      description: ""
      responses:
        2XX:
          description: '{@link JAXBElement} with given VM group name'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: groupName
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/namedVmGroupReferences:
    get:
      tags:
        - extension
      description: Gets all references to named VM groups.
      responses:
        2XX:
          description: '{@link JAXBElement} with given VM group name'
      security:
        - bearerAuth: []
  /vApp/{id}/virtualHardwareSection/networkCards:
    get:
      tags:
        - user
      description: Retrieve all RASD items that specify network card properties of a VM.
      responses:
        2XX:
          description: RasdItemsListType
          content:
            application/vnd.vmware.vcloud.rasdItemsList+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_RasdItemsListType'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update all RASD items that specify network card properties of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/virtualHardwareSection/networkCards:
    get:
      tags:
        - user
      description: Retrieve all RASD items that specify network card properties of a VM.
      responses:
        2XX:
          description: RasdItemsListType
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update all RASD items that specify network card properties of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/networkConfigSection:
    get:
      tags:
        - user
      description: Retrieve the network config section of a vApp or vApp template.
      responses:
        2XX:
          description: NetworkConfigSectionType
          content:
            application/vnd.vmware.vcloud.networkConfigSection+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_NetworkConfigSectionType'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update the network config section of a vApp.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/networkConfigSection:
    get:
      tags:
        - user
      description: Retrieve the network config section of a vApp or vApp template.
      responses:
        2XX:
          description: NetworkConfigSectionType
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update the network config section of a vApp.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/networkConnectionSection:
    get:
      tags:
        - user
      description: Retrieve the network connection section of a VM.
      responses:
        2XX:
          description: NetworkConnectionSectionType
          content:
            application/vnd.vmware.vcloud.networkConnectionSection+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_NetworkConnectionSectionType'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update the network connection section of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/networkConnectionSection:
    get:
      tags:
        - user
      description: Retrieve the network connection section of a VM.
      responses:
        2XX:
          description: NetworkConnectionSectionType
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update the network connection section of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/network/{id}/metadata:
    get:
      tags:
        - admin
      description: Retrieve metadata associated with the network.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Merge the metadata provided in the request with existing metadata.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /network/{id}/metadata:
    get:
      tags:
        - user
      description: Retrieve metadata associated with the network.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/network/{id}/metadata/{domain}/{key}:
    get:
      tags:
        - admin
      description: Retrieve the value of the specified key in the specified domain from network metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: domain
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/networkPoolReferences:
    get:
      tags:
        - extension
      description: Retrieve a list of references to network pools in the system.
      responses:
        2XX:
          description: VMWNetworkPoolReferences
      deprecated: true
      security:
        - bearerAuth: []
  /admin/extension/networkPoolReferences/query:
    get:
      tags:
        - extension
      description: Get list of network pools by using REST API general QueryHandler. This is read only list of network pool references and is not bound to specific states. If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - referenceView or simpleView. Default format is referenceView.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      deprecated: true
      security:
        - bearerAuth: []
  /vApp/{id}/networkSection:
    get:
      tags:
        - user
      description: Retrieve the network section of a vApp or vApp template.
      responses:
        2XX:
          description: ovf:NetworkSection\_Type
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/networkSection:
    get:
      tags:
        - user
      description: Retrieve the network section of a vApp or vApp template.
      responses:
        2XX:
          description: ovf:NetworkSection\_Type
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/settings/notifications:
    get:
      tags:
        - extension
      description: Retrieve system AMQP notifications settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update system AMQP notifications settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
  /admin/extension/nsxtManagers:
    get:
      tags:
        - extension
      description: Retrieve an existing NSX-T Manager.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    post:
      tags:
        - extension
      description: Register an NSX-T Manager.
      responses:
        2XX:
          description: A newly created NSX-T Manager
      security:
        - bearerAuth: []
  /nsxtManagers:
    get:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    post:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
  /admin/extension/object:
    get:
      tags:
        - extension
      description: Displays the list of currently registered object extensions
      responses:
        2XX:
          description: Representation of a collection of registered object extensions {@link ObjectExtensionsType}
      deprecated: true
      security:
        - bearerAuth: []
    post:
      tags:
        - extension
      description: Registers a new object extension
      responses:
        2XX:
          description: representation of an object extension {@link ObjectExtensionType}
      deprecated: true
      security:
        - bearerAuth: []
  /vApp/{id}/operatingSystemSection:
    get:
      tags:
        - user
      description: Retrieve the operating system section of a VM.
      responses:
        2XX:
          description: ovf:OperatingSystemSection\_Type
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update the operating system section of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/settings/operationLimitsSettings:
    get:
      tags:
        - extension
      description: Retrieves the system wide defaults for tenant throttling
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update catalog settings details.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
  /admin/org/{id}/settings/email:
    get:
      tags:
        - admin
      description: Retrieve organization email settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update organization email settings.
      responses:
        2XX:
          description: JAXBElement
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/settings/guestPersonalizationSettings:
    get:
      tags:
        - admin
      description: Retrieve organization defaults for guest personalization settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update organization defaults for guest personalization settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/settings/ldap:
    get:
      tags:
        - admin
      description: Retrieve organization LDAP settings. This API is under accelerated deprecation and will be removed more quickly than in the usual deprecation cycle. The preferred method for managing organization LDAP settings is using the OrgSettings API.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      deprecated: true
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update organization LDAP settings. This API is under accelerated deprecation and will be removed more quickly than in the usual deprecation cycle. The preferred method for managing organization LDAP settings is using the OrgSettings API.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/orgNetworks/query:
    get:
      tags:
        - extension
      description: Get list of Organization Networks for the system by using REST API general QueryHandler. This is read only list and is not bound to specific states. If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - referenceView or simpleView. Default format is referenceView.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      deprecated: true
      security:
        - bearerAuth: []
  /admin/org/{id}/settings/operationLimitsSettings:
    get:
      tags:
        - admin
      description: Retrieve organization defaults for guest personalization settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update organization operation limits settings.
      responses:
        2XX:
          description: JAXBElement
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/settings/passwordPolicy:
    get:
      tags:
        - admin
      description: Retrieve organization password policy settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update organization password policy settings.
      responses:
        2XX:
          description: JAXBElement
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/settings:
    get:
      tags:
        - admin
      description: |-
        Retrieve settings for this organization.

         Organization settings are divided into categories. This request retrieves all categories of organization settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: |-
        Updates settings for this organization.

         Organization settings are divided into categories. This request updates all categories of organization settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/vdc/{id}/edgeGateways:
    get:
      tags:
        - admin
      description: List all gateways for this Org vDC.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.query.records+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_QueryResultRecordsType'
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Create an edge gateway. API Version 35.0 is the last version this API is supported for NSX-T Edges.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.admin.edgeGateway+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_GatewayType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/vdc/{id}/networks:
    get:
      tags:
        - admin
      description: List all networks for this Org vDC.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.query.references+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_ReferencesType'
      deprecated: true
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Create an Org vDC network.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /org/{id}/vdcRollup:
    get:
      tags:
        - user
      description: Retrieve the summary of usage and reserve capacity of all OrgVDCs in this org, and, if specified, any associated orgs.
      responses:
        2XX:
          description: The {@link JAXBElement} of the {@link OrgVdcRollupType}.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/orgVdcs/query:
    get:
      tags:
        - extension
      description: Get list of all Organization Vdcs in the system by using REST API general QueryHandler. This is read only list and is not bound to specific states. If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - referenceView or simpleView. Default format is referenceView.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      security:
        - bearerAuth: []
  /org/{id}:
    get:
      tags:
        - user
      description: Retrieve an organization.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.org+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_OrgType'
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/metadata:
    get:
      tags:
        - admin
      description: Retrieve metadata associated with the organization.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Merge the metadata provided in the request with existing metadata.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /org/{id}/metadata:
    get:
      tags:
        - user
      description: Retrieve metadata associated with the organization.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/metadata/{domain}/{key}:
    get:
      tags:
        - admin
      description: Retrieve the value of the specified key in the specified domain from organization metadata .
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: domain
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vdc/{id}/resourcePools:
    get:
      tags:
        - extension
      description: Retrieve resource pools associated with an organization VDC
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /org:
    get:
      tags:
        - user
      description: |-
        Retrieve a list of organizations accessible to you.

         The system administrator has access to all organizations.
      responses:
        2XX:
          description: OrgListType
          content:
            application/vnd.vmware.vcloud.orgList+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_OrgListType'
      deprecated: true
      security:
        - bearerAuth: []
  /admin/orgs/query:
    get:
      tags:
        - admin
      description: Retrieves a list of organizations by using REST API general QueryHandler; If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - references or records. Default format is references.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      deprecated: true
      security:
        - bearerAuth: []
  /vApp/{id}/productSections:
    get:
      tags:
        - user
      description: Retrieve a list of ProductSection elements from a vApp template, vApp, or VM.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update all ProductSection elements in a vApp template, vApp, or VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/productSections:
    get:
      tags:
        - user
      description: Retrieve a list of ProductSection elements from a vApp template, vApp, or VM.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update all ProductSection elements in a vApp template, vApp, or VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/providervdc/{id}:
    get:
      tags:
        - admin
      description: Retrieve a provider vDC.
      responses:
        2XX:
          description: ProviderVdcType
          content:
            application/vnd.vmware.admin.providervdc+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_ProviderVdcType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/providervdc/{id}/metadata:
    get:
      tags:
        - admin
      description: Retrieve metadata associated with the provider vDC.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Merge the metadata provided in the request with existing metadata.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/providerVdcReferences:
    get:
      tags:
        - extension
      description: Retrieve a list of references to provider vDCs in the system.
      responses:
        2XX:
          description: VMWProviderVdcReferences
      security:
        - bearerAuth: []
  /admin/extension/pvdcStorageProfile/{id}:
    get:
      tags:
        - extension
      description: Retrieve provider vDC storage profile details.
      responses:
        2XX:
          description: ProviderVdcStorageClassType representation of storage profile referred by storageClassId
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: |-
        Update a provider vDC storage profile

         You can update a storage profile to enable or disable it.
      responses:
        2XX:
          description: Updated representation of the storage profile
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/pvdcStorageProfile/{id}:
    get:
      tags:
        - admin
      description: Retrieve provider vDC storage profile details.
      responses:
        2XX:
          description: ProviderVdcStorageClassType representation of storage profile referred by storageClassId
          content:
            application/vnd.vmware.admin.pvdcStorageProfile+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_ProviderVdcStorageProfileType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/pvdcStorageProfile/{id}/metadata:
    get:
      tags:
        - admin
      description: Retrieve metadata associated with the Provider VDC Storage Profile.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Merge the metadata provided in the request with existing metadata.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/providerVdcReferences/query:
    get:
      tags:
        - extension
      description: Get list of provider vDCs by using REST API general QueryHandler. This is read only list and is not bound to specific states. If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - references, records or idrecords. Default format is records.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      security:
        - bearerAuth: []
  /admin/extension/providervdc/{id}/resourcePools:
    get:
      tags:
        - extension
      description: Retrieve a provider vDC resource pool set.
      responses:
        2XX:
          description: '{@link PvdcResourcePoolSetType}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /service:
    get:
      tags:
        - user
      description: Return all extension services registered with the system.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.query.records+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_QueryResultRecordsType'
      deprecated: true
      security:
        - bearerAuth: []
  /admin/user/{id}/grantedRights:
    get:
      tags:
        - admin
      description: Queries user granted privileges
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/resourceclass/{id}/resourceclassactions:
    get:
      tags:
        - extension
      description: List registered resource class actions for extension service.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.query.records+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_QueryResultRecordsType'
      security:
        - bearerAuth: []
    post:
      tags:
        - extension
      description: Registers resource class action for extension service. The resource class action will be added only if a resource class with the same name does not exist.
      responses:
        2XX:
          description: Newly registered or already existing resource class action.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/{id}/resourceclasses:
    get:
      tags:
        - extension
      description: List registered resource classes for extension service.
      responses:
        2XX:
          description: JAXB resource class
      security:
        - bearerAuth: []
    post:
      tags:
        - extension
      description: Registers resource class for extension service. The resource class will be added only if a resource class with the same name does not exist.
      responses:
        2XX:
          description: Newly registered or already existing resource class.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/resourcePool/{rpId}:
    get:
      tags:
        - extension
      description: Gets {@link JAXBElement} with given resource pool id.
      responses:
        2XX:
          description: '{@link JAXBElement} with given resource pool id.'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: rpId
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/{id}/rights:
    get:
      tags:
        - extension
      description: Gives the rights registered by this extension service.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    post:
      tags:
        - extension
      description: Registers right to the rights managed by this extension service. The right will be added only if a right with the same name does not already exists.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.admin.right+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_RightType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/vdcs/query:
    get:
      tags:
        - admin
      description: Retrieves a list of vdcs in the organization; If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - references or records. Default format is references.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      security:
        - bearerAuth: []
  /admin/org/{orgId}/roles/query:
    get:
      tags:
        - admin
      description: Retrieves a list of roles in the cloud by using REST API general QueryHandler; If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - references or records. Default format is references.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: orgId
        required: true
        schema:
          type: string
        style: simple
  /admin/roles/query:
    get:
      tags:
        - admin
      description: Retrieves a list of roles in the cloud by using REST API general QueryHandler; If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - references or records. Default format is references.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      deprecated: true
      security:
        - bearerAuth: []
  /admin/extension/resourcePool/{rpId}/rules:
    get:
      tags:
        - extension
      description: List all VmHostAffinityRules within this resource pool.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    post:
      tags:
        - extension
      description: Create VM Affinity Rules within this resource pool.
      responses:
        2XX:
          description: the task to create VM Host Affinity Rule
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: rpId
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/runtimeInfoSection:
    get:
      tags:
        - user
      description: Retrieve the runtime info section of a VM.
      responses:
        2XX:
          description: RuntimeInfoSectionType The runtime info section
          content:
            application/vnd.vmware.vcloud.runtimeInfoSection+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_RuntimeInfoSectionType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /v1.5/schema/{schemaFileName}:
    get:
      tags:
        - user
      description: Retrieve the specified schema definition file from the server.
      responses:
        2XX:
          description: xml schema represented as String
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: schemaFileName
        required: true
        schema:
          type: string
        style: simple
  /versions/schema/versions.xsd:
    get:
      tags:
        - user
      description: Unauthenticated GET of versions.xsd schema definition file.
      responses:
        2XX:
          description: versions.xsd encoded as UTF-8
      security:
        - bearerAuth: []
  /schema:
    get:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
  /admin/catalog/{id}/extension:
    get:
      tags:
        - admin
      description: Displays a list of Selector Extensions that are associated with a selector.
      responses:
        2XX:
          description: Representation of a collection of selector extension {@link SelectorExtensionsType}
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Creates a Selector Extension.
      responses:
        2XX:
          description: Representation of a Selector Extension {@link SelectorExtensionType}
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/vdcs/extension:
    get:
      tags:
        - admin
      description: Displays a list of Selector Extensions that are associated with a selector.
      responses:
        2XX:
          description: Representation of a collection of selector extension {@link SelectorExtensionsType}
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Creates a Selector Extension.
      responses:
        2XX:
          description: Representation of a Selector Extension {@link SelectorExtensionType}
      security:
        - bearerAuth: []
  /admin/org/{id}/extension:
    get:
      tags:
        - admin
      description: Displays a list of Selector Extensions that are associated with a selector.
      responses:
        2XX:
          description: Representation of a collection of selector extension {@link SelectorExtensionsType}
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Creates a Selector Extension.
      responses:
        2XX:
          description: Representation of a Selector Extension {@link SelectorExtensionType}
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/providervdc/{id}/extension:
    get:
      tags:
        - admin
      description: Displays a list of Selector Extensions that are associated with a selector.
      responses:
        2XX:
          description: Representation of a collection of selector extension {@link SelectorExtensionsType}
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Creates a Selector Extension.
      responses:
        2XX:
          description: Representation of a Selector Extension {@link SelectorExtensionType}
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/vdc/{id}/extension:
    get:
      tags:
        - admin
      description: Displays a list of Selector Extensions that are associated with a selector.
      responses:
        2XX:
          description: Representation of a collection of selector extension {@link SelectorExtensionsType}
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Creates a Selector Extension.
      responses:
        2XX:
          description: Representation of a Selector Extension {@link SelectorExtensionType}
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/object/{id}/selectorExtensions:
    get:
      tags:
        - extension
      description: Displays a list of all selector extensions of a registered object extension.
      responses:
        2XX:
          description: Representation of a collection of {@link SelectorExtensionType} associated with a. object extension
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/virtualHardwareSection/serialPorts:
    get:
      tags:
        - user
      description: Retrieve all RASD items that specify serial port properties of a VM.
      responses:
        2XX:
          description: RasdItemsListType
          content:
            application/vnd.vmware.vcloud.rasdItemsList+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_RasdItemsListType'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Modifies all RASD items that specify serial port properties of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/virtualHardwareSection/serialPorts:
    get:
      tags:
        - user
      description: Retrieve all RASD items that specify serial port properties of a VM.
      responses:
        2XX:
          description: RasdItemsListType
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Modifies all RASD items that specify serial port properties of a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /server_status:
    get:
      tags:
        - user
      description: Always returns HTTP 200
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
  /service/{id}:
    get:
      tags:
        - user
      description: Return the Extension Service API Definition.
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/resourceclass/{id}/serviceresources:
    get:
      tags:
        - extension
      description: List registered service resources for extension service.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.query.records+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_QueryResultRecordsType'
      security:
        - bearerAuth: []
    post:
      tags:
        - extension
      description: Registers service resource for extension service.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/query:
    get:
      tags:
        - extension
      description: Return the System Administrator representation of all extension services registered with the system.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
  /vAppTemplate/{id}/shadowVms:
    get:
      tags:
        - user
      description: Retrieve a list of shadow VMs associated with the specified vApp template.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /site/associations/localAssociationData:
    get:
      tags:
        - user
      description: Get the {@link SiteAssociationsType} for this site. GET /api/site/associations/localAssociationData
      responses:
        2XX:
          description: The site association for this site that could be used with a remote site
      security:
        - bearerAuth: []
  /site/associations:
    get:
      tags:
        - user
      description: Get the {@link SiteAssociationsType} for this site. GET /api/site/associations
      responses:
        2XX:
          description: The site associations for the site
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update the site's list of associated sites PUT /api/site/associations
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    post:
      tags:
        - user
      description: Add a new {@link SiteAssociationType} for this site. POST /api/site/associations
      responses:
        2XX:
          description: JAXBElement
      security:
        - bearerAuth: []
  /vApp/{id}/snapshotSection:
    get:
      tags:
        - user
      description: Retrieve SnapshotSection element for a vApp or VM.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/startupSection:
    get:
      tags:
        - user
      description: Retrieve the startup section of a vApp.
      responses:
        2XX:
          description: ovf:StartupSection\_Type
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update the startup section of a vApp.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vimServer/{id}/storageProfiles:
    get:
      tags:
        - extension
      description: Retrieve all storage profiles in the VIM server
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/strandedItems/query:
    get:
      tags:
        - extension
      description: Get list of all stranded items in the system by using REST API general QueryHandler. This is read only list and is not bound to specific states. If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - references, records or idrecords. Default format is records.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      security:
        - bearerAuth: []
  /versions:
    get:
      tags:
        - user
      description: Retrieve a list of vCloud API versions supported by this system. This request does not require an authentication token.
      responses:
        2XX:
          description: The list of supported vCloud API versions and their login URLs.
          content:
            application/*+json;version=39.0:
              schema:
                $ref: '#/components/schemas/versioning_SupportedVersionsType'
      security:
        - bearerAuth: []
  /admin/extension/settings/passwordPolicy:
    get:
      tags:
        - extension
      description: Retrieve system password policy settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update system password policy settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
  /admin/extension/settings:
    get:
      tags:
        - extension
      description: |-
        Retrieve system settings.

         System settings specify default values and behaviors for the cloud and all of the organizations in it. System settings are divided into categories. This request retrieves all categories of system settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: |-
        Update system settings.

         System settings specify default values and behaviors for the cloud and all of the organizations in it. System settings are divided into categories. This request updates all categories of system settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
  /task/{id}:
    get:
      tags:
        - user
      description: Retrieve a task.
      responses:
        2XX:
          description: TaskType
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Update a task.
      responses:
        2XX:
          description: '{@link TaskType}'
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /tasksList/{id}:
    get:
      tags:
        - user
      description: Retrieve a list of this organization's queued, running, or recently completed tasks.
      responses:
        2XX:
          description: TasksListType
      security:
        - bearerAuth: []
    post:
      tags:
        - user
      description: Post an external task to this organization.
      responses:
        2XX:
          description: '{@link TaskType} since 2.0'
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/screen:
    get:
      tags:
        - user
      description: Retrieves a thumbnail image of a VM console. The content type of the response can be any of {@code image/png}, {@code image/gif}.
      responses:
        2XX:
          description: byte data
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/trustedPlatformModule:
    get:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/users/query:
    get:
      tags:
        - admin
      description: Retrieves a list of users for organization the org admin belongs to by using REST API general QueryHandler; If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - references or records. Default format is references.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      deprecated: true
      security:
        - bearerAuth: []
  /admin/org/{id}/settings/vAppLeaseSettings:
    get:
      tags:
        - admin
      description: Retrieve organization vApp lease settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update organization vApp lease settings.
      responses:
        2XX:
          description: JAXBElement
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/metadata:
    get:
      tags:
        - user
      description: Retrieve metadata associated with the vApp or VM.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    post:
      tags:
        - user
      description: Merge the metadata provided in the request with existing metadata.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/ovf:
    get:
      tags:
        - user
      description: Retrieve the OVF descriptor of a vApp directly.
      responses:
        2XX:
          description: ovf:EnvelopType
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/owner:
    get:
      tags:
        - user
      description: Retrieve the owner of a vApp.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Change the owner of a vApp.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/customizationSection:
    get:
      tags:
        - user
      description: Retrieve the customization section of a vApp template.
      responses:
        2XX:
          description: CustomizationSectionType
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/settings/vAppTemplateLeaseSettings:
    get:
      tags:
        - admin
      description: Retrieve organization vApp template lease settings.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update organization vApp template lease settings.
      responses:
        2XX:
          description: JAXBElement
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/metadata:
    get:
      tags:
        - user
      description: Retrieve metadata associated with the vApp template or VM.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    post:
      tags:
        - user
      description: Merge the metadata provided in the request with existing metadata.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/ovf:
    get:
      tags:
        - user
      description: Retrieve the OVF descriptor of a vApp template.
      responses:
        2XX:
          description: ovf:EnvelopType
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/owner:
    get:
      tags:
        - user
      description: Retrieve the owner of a vApp template.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Change the owner of a vApp Template.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplates/query:
    get:
      tags:
        - user
      description: Retrieves a list of vAppTemplates using REST API general QueryHandler; If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - references or records. Default format is references.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      security:
        - bearerAuth: []
  /admin/extension/vapps/query:
    get:
      tags:
        - extension
      description: Get list of all vApps in the system by using REST API general QueryHandler. This is read only list and is not bound to specific states. If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - referenceView or simpleView. Default format is referenceView.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      security:
        - bearerAuth: []
  /vApps/query:
    get:
      tags:
        - user
      description: Retrieves a list of vApps by using REST API general QueryHandler; If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - references or records. Default format is references.
      responses:
        2XX:
          description: Returns OkResponse
      security:
        - bearerAuth: []
  /vms/query:
    get:
      tags:
        - user
      description: Retrieves a list of VMs in lease by using REST API general QueryHandler; If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - references or records. Default format is references.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse
      security:
        - bearerAuth: []
  /admin/extension/vsm/{id}:
    get:
      tags:
        - extension
      description: Retrieve vShield Manager configuration details, including administrator credentials.
      responses:
        2XX:
          description: JAXB representation of vShieldManager.
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update vShield Manager configuration details, including administrator credentials.
      responses:
        2XX:
          description: Reference to the started update VSM task.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vimServer/{id}/{vimObjType}/{vimObjMoref}/vSphereWebClientUrl:
    get:
      tags:
        - extension
      description: Retrieve the vSphere URL of an object.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: vimObjType
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: vimObjMoref
        required: true
        schema:
          type: string
        style: simple
  /admin/:
    get:
      tags:
        - admin
      description: |-
        Retrieve an administrative view of a cloud.

         The VCloud element provides access to cloud-wide namespace of objects that an administrator can view and, in most cases, modify.
      responses:
        2XX:
          description: VCloudType
          content:
            application/vnd.vmware.admin.vcloud+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_VCloudType'
      security:
        - bearerAuth: []
  /admin/vdc/{vdcId}/computePolicies:
    get:
      tags:
        - admin
      description: Get all vDC compute policies references in given vDC
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Update vDC compute policies given vDC
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: vdcId
        required: true
        schema:
          type: string
        style: simple
  /vdc/{vdcId}/computePolicies:
    get:
      tags:
        - user
      description: Get all vDC compute policies references in given vDC
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: vdcId
        required: true
        schema:
          type: string
        style: simple
  /org/{id}/metrics/historic:
    get:
      tags:
        - user
      description: Retrieve the all the metrics for a given org for the past 24 hours
      responses:
        2XX:
          description: rolled up metrics for the organization
      security:
        - bearerAuth: []
    post:
      tags:
        - user
      description: Retrieve the metrics for a given org based on the filters provided by the spec
      responses:
        2XX:
          description: rolled up metrics for the organization
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}/metrics/historic:
    get:
      tags:
        - user
      description: Retrieves the past 24h rolled up metrics for a VDC
      responses:
        2XX:
          description: rolled up metrics for the vdc
      security:
        - bearerAuth: []
    post:
      tags:
        - user
      description: Retrieves rolled up metrics for a VDC according to the spec
      responses:
        2XX:
          description: Rolled up metrics for the VDC
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/vdc/{id}/metadata:
    get:
      tags:
        - admin
      description: Retrieve metadata associated with the virtual datacenter.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Merge the metadata provided in the request with existing metadata.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}/metadata:
    get:
      tags:
        - user
      description: Retrieve metadata associated with a vDC.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/vdc/{id}/metadata/{domain}/{key}:
    get:
      tags:
        - admin
      description: Retrieve the value of the specified key in the specified domain from virtual datacenter metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: domain
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /vdcStorageProfile/{id}:
    get:
      tags:
        - user
      description: Returns storage class referred by the Id. All properties of the storage classes are visible to vcloud user, except for VDC Storage Class reference
      responses:
        2XX:
          description: VdcStorageClassType representation of storage class referred by storageClassId
          content:
            application/vnd.vmware.vcloud.vdcStorageProfile+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_VdcStorageProfileType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/vdcStorageProfile/{id}/metadata:
    get:
      tags:
        - admin
      description: Retrieve metadata associated with the vDC storage profile.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    post:
      tags:
        - admin
      description: Merge the metadata provided in the request with existing metadata.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdcStorageProfile/{id}/metadata:
    get:
      tags:
        - user
      description: Retrieve metadata associated with the vDC storage profile.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/vdcStorageProfile/{id}/metadata/{domain}/{key}:
    get:
      tags:
        - admin
      description: Retrieve the value of the specified key from vDC storage profile metadata.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: domain
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: key
        required: true
        schema:
          type: string
        style: simple
  /admin/vdcStorageProfile/{id}:
    get:
      tags:
        - admin
      description: Returns the representation of a vDC storage profile
      responses:
        2XX:
          description: AdminVdcStorageProfileType representation of storage profile referred by storageProfileId
          content:
            application/vnd.vmware.admin.vdcStorageProfile+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_AdminVdcStorageProfileType'
      security:
        - bearerAuth: []
    put:
      tags:
        - admin
      description: Updates a single storage profile with user supplied data
      responses:
        2XX:
          description: Updated representation of the storage profile
          content:
            application/vnd.vmware.admin.vdcStorageProfile+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_AdminVdcStorageProfileType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdcTemplate/{id}:
    get:
      tags:
        - user
      description: Retrieve a vDC template.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/providervdc/{id}/vdcReferences:
    get:
      tags:
        - admin
      description: |-
        Retrieve a list of organization vDCs supported by a given provider vDC.

         Resources from the provider vDC are allocated across all organization vDCs in the list.
      responses:
        2XX:
          description: VdcReferencesType
          content:
            application/vnd.vmware.admin.vdcReferences+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_VdcReferencesType'
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/networkPool/{id}/vendorServices:
    get:
      tags:
        - extension
      description: Retrieve services associated with this network pool
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vimServerReferences:
    get:
      tags:
        - extension
      description: Retrieve a list of references to vCenter servers attached to the system.
      responses:
        2XX:
          description: VMWVimServerReferences
      security:
        - bearerAuth: []
  /admin/extension/vimServerReferences/query:
    get:
      tags:
        - extension
      description: Get list of vSphere servers by using REST API general QueryHandler; This is read only list of vim servers and is not bound to specific states. If filter is provided it will be applied to the corresponding result set. Format determines the elements representation - referenceView or simpleView. Default format is referenceView.
      parameters:
        - in: query
          name: filter
          schema:
            $ref: '#/components/schemas/query-parameter_filter'
          style: form
        - in: query
          name: sortAsc
          schema:
            $ref: '#/components/schemas/query-parameter_sortAsc'
          style: form
        - in: query
          name: sortDesc
          schema:
            $ref: '#/components/schemas/query-parameter_sortDesc'
          style: form
        - in: query
          name: format
          schema:
            $ref: '#/components/schemas/query-parameter_format'
          style: form
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
        - in: query
          name: offset
          schema:
            $ref: '#/components/schemas/query-parameter_offset'
          style: form
        - in: query
          name: fields
          schema:
            $ref: '#/components/schemas/query-parameter_fields'
          style: form
        - in: query
          name: links
          schema:
            $ref: '#/components/schemas/query-parameter_links'
          style: form
      responses:
        2XX:
          description: Returns OkResponse;
      security:
        - bearerAuth: []
  /admin/extension/vimServer/{id}:
    get:
      tags:
        - extension
      description: Retrieve the representation of a registered vCenter server.
      responses:
        2XX:
          description: VimServerType
      deprecated: true
      security:
        - bearerAuth: []
    put:
      tags:
        - extension
      description: Update the representation of a registered vCenter server. This will not make any edits to, add, or remove the vCenter's Shield Manager. If you would like to do this, use cloud API's PUT /1.0.0/virtualCenters/{vcUrn}
      responses:
        2XX:
          description: TaskType This operation is asynchronous and the user should monitor the returned task status in order to check when it is completed.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/virtualHardwareSection:
    get:
      tags:
        - user
      description: |-
        Retrieve the virtual hardware section of a VM.

         This operation retrieves the entire VirtualHardwareSection of a VM. You can also retrieve many RASD item elements of a VirtualHardwareSection individually, or as groups of related items.
      responses:
        2XX:
          description: ovf:VirtualHardwareSection\_Type
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: |-
        Update the virtual hardware section of a VM.

         This operation updates the entire VirtualHardwareSection of a VM. You can also update many RASD item elements of a VirtualHardwareSection individually, or as groups of related items.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/virtualHardwareSection:
    get:
      tags:
        - user
      description: |-
        Retrieve the virtual hardware section of a VM.

         This operation retrieves the entire VirtualHardwareSection of a VM. You can also retrieve many RASD item elements of a VirtualHardwareSection individually, or as groups of related items.
      responses:
        2XX:
          description: ovf:VirtualHardwareSection\_Type
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: |-
        Update the virtual hardware section of a VM.

         This operation updates the entire VirtualHardwareSection of a VM. You can also update many RASD item elements of a VirtualHardwareSection individually, or as groups of related items.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}/vmAffinityRules:
    get:
      tags:
        - user
      description: Retrieves a list of all VM Affinity rules within a given vDC.
      responses:
        2XX:
          description: a list of all VM Affinity rules( both affinity and anti-affinity) for the given vDC
      security:
        - bearerAuth: []
    post:
      tags:
        - user
      description: Creates a new VM affinity rule.This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      responses:
        2XX:
          description: representation of a Task {@link TaskSpec}
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/vmCapabilities:
    get:
      tags:
        - user
      description: Gets {@link VmCapabilitiesType} for the VM identified by id.
      responses:
        2XX:
          description: '{@link VmCapabilitiesType}'
      security:
        - bearerAuth: []
    put:
      tags:
        - user
      description: Updates VM's {@link VmCapabilities}
      responses:
        2XX:
          description: '{@link AcceptedResponse}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/complianceResult:
    get:
      tags:
        - user
      description: Return the results of the most recent POST /vApp/vm-{id}/action/checkCompliance request to the specified VM.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vm.complianceResult+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_ComplianceResultType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/metrics/current:
    get:
      tags:
        - user
      description: Gets the current usage for all available VM metrics.
      responses:
        2XX:
          description: a {@link CurrentUsageType} instance of metrics for the specified VM
      security:
        - bearerAuth: []
    post:
      tags:
        - user
      description: |-
        Gets the current usage for VM metrics according to the provided spec object.

         The spec object can contain a collection of wildcard-style metric name patterns to fine-tune the list of returned metrics.
      responses:
        2XX:
          description: a {@link CurrentUsageType} instance of metrics for the specified VM
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/resourcePool/{rpId}/vmGroup/name/{groupName}:
    get:
      tags:
        - extension
      description: Gets {@link JAXBElement} with given VM group name.
      responses:
        2XX:
          description: '{@link JAXBElement} with given VM group name'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: rpId
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: groupName
        required: true
        schema:
          type: string
        style: simple
  /vmGroup/name/{groupName}:
    get:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: groupName
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vmGroups/name/{groupName}:
    get:
      tags:
        - extension
      description: ""
      responses:
        2XX:
          description: '{@link JAXBElement} with given VM group name'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: groupName
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/resourcePool/{rpId}/vmGroups:
    get:
      tags:
        - extension
      description: List all VmGroups within this resource pool.
      responses:
        2XX:
          description: list of VM groups in given resource pool.
      security:
        - bearerAuth: []
    post:
      tags:
        - extension
      description: Create VM group within this resource pool.
      responses:
        2XX:
          description: VM group task
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: rpId
        required: true
        schema:
          type: string
        style: simple
  /vmGroups:
    get:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    post:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
  /vApp/{id}/metrics/historic:
    get:
      tags:
        - user
      description: Gets the past 24 hours worth of samples for all available VM metrics.
      responses:
        2XX:
          description: a {@link HistoricUsageType} instance of metrics for the specified VM
      security:
        - bearerAuth: []
    post:
      tags:
        - user
      description: |-
        Gets the historic usage for VM metrics according to the provided spec object.

         The spec object can contain a collection of wildcard-style metric name patterns to fine-tune the list of returned metrics, as well as a start and end time (either relative or absolute) to refine the set of sample points to a specific date range.
      responses:
        2XX:
          description: a {@link HistoricUsageType} instance of metrics for the specified VM
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/question:
    get:
      tags:
        - user
      description: Retrieve a question being asked by a VM.
      responses:
        2XX:
          description: VmPendingQuestionType
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /media/{id}/attachedVms:
    get:
      tags:
        - user
      description: Retrieve a list of all the VMs attached to the media.
      responses:
        2XX:
          description: '{@link JAXBElement\< VmsType \>}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /disk/{id}/attachedVms:
    get:
      tags:
        - user
      description: Retrieve a list of all VMs attached to a disk.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/resourcePool/{id}/vmList:
    get:
      tags:
        - extension
      description: List all VMs using this resource pool.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vimServer/{id}/vmsList:
    get:
      tags:
        - extension
      description: |-
        Retrieve a list of virtual machines that can be imported from a registered vCenter server.

         This request supports optional query parameters:

        * page - The page to return. The first page is page 1.
        * pageSize - The number of virtual machines to list on a page. An integer value between 1 and 100. The default value is 1.
      parameters:
        - in: query
          name: page
          schema:
            $ref: '#/components/schemas/query-parameter_page'
          style: form
        - in: query
          name: pageSize
          schema:
            $ref: '#/components/schemas/query-parameter_pageSize'
          style: form
      responses:
        2XX:
          description: VmObjectRefsListType
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vmGroup/{groupId}/vmsList:
    get:
      tags:
        - extension
      description: Enumerate the properties of the specified VM group
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: groupId
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vmGroup/name/{groupName}/vmsList:
    get:
      tags:
        - extension
      description: Get all VMs from all clusters in given VM group.
      responses:
        2XX:
          description: list of VM records.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: groupName
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/blockingTask/{id}/action/abort:
    post:
      tags:
        - extension
      description: |-
        Abort a blocking task.

         This request ends the task, cleaning up any transient objects that it created. Task status is set to ABORTED.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/screen/action/acquireMksTicket:
    post:
      tags:
        - user
      description: Retrieve a mks ticket that you can use to gain access to the console of a running VM.
      responses:
        2XX:
          description: '{@link MksTicketType}'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/screen/action/acquireTicket:
    post:
      tags:
        - user
      description: Retrieve a screen ticket that you can use with the VMRC browser plug-in to gain access to the console of a running VM.
      responses:
        2XX:
          description: ScreenTicketType
          content:
            application/vnd.vmware.vcloud.screenTicket+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_ScreenTicketType'
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vmGroup/name/{groupName}/action/addVms:
    post:
      tags:
        - extension
      description: |-
        Adds virtual machines to the specified VM group.

         This asynchronous call returns a task. The process of adding virtual machines to a VM group can result in the virtual machine being processed and redeployed by the placement engine. All virtual machines provided to this call will be marked busy until the add to group process is complete.
      responses:
        2XX:
          description: a task to monitor the progress of adding virtual machines to a VM group
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: groupName
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/question/action/answer:
    post:
      tags:
        - user
      description: Answer a question being asked by a VM.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/disk/action/attach:
    post:
      tags:
        - user
      description: Attach a disk to a VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /task/{id}/action/cancel:
    post:
      tags:
        - user
      description: Cancel a task.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /catalog/{id}/action/captureVApp:
    post:
      tags:
        - user
      description: Create a vApp template in this catalog from a vApp.
      responses:
        2XX:
          description: |-
            TaskType

             The response includes a Task element. You can monitor the task to to track the creation of the vApp template.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}/action/captureVApp:
    post:
      tags:
        - user
      description: Create a vApp template from a vApp.
      responses:
        2XX:
          description: |-
            VAppTemplateType

             The response includes a Task element. You can monitor the task to to track the creation of the vApp template.
          content:
            application/vnd.vmware.vcloud.vAppTemplate+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_VAppTemplateType'
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/changeReplicationTrackingSettings:
    post:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/{id}/authorizationcheck:
    post:
      tags:
        - extension
      description: Checks user authorization for service, URL and request verb.
      responses:
        2XX:
          description: AuthorizationCheckResponseType
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/authorizationcheck:
    post:
      tags:
        - extension
      description: Checks user authorization for all services with enabled authorization, URL and request verb.
      responses:
        2XX:
          description: AuthorizationCheckResponseType
      security:
        - bearerAuth: []
  /vApp/{id}/action/checkComputePolicyCompliance:
    post:
      tags:
        - user
      description: Check VM's compute policy compliance
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/checkCompliance:
    post:
      tags:
        - user
      description: Check storage profile compliance for the specified VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task. When the task completes, you can retrieve the results by making a GET /vApp/vm-{id}/complianceResult request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/service/action/clearunusedlocalizationbundles:
    post:
      tags:
        - extension
      description: Cleanup for unused external localization resources.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
  /admin/extension/service/action/clearunusedrights:
    post:
      tags:
        - extension
      description: Clears all the rights that are not associated with a role or acl rule and which extension service is already deleted.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
  /vdc/{id}/action/cloneMedia:
    post:
      tags:
        - user
      description: Create a copy of a media object.
      responses:
        2XX:
          description: |-
            MediaType

             The response includes a Task element. You can monitor the task to to track the creation of the copy.
          content:
            application/vnd.vmware.vcloud.media+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_MediaType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}/action/cloneVApp:
    post:
      tags:
        - user
      description: Create a copy of a vApp.
      responses:
        2XX:
          description: |-
            VAppType

             The response includes a Task element. You can monitor the task to to track the creation of the copy.
          content:
            application/vnd.vmware.vcloud.vApp+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_VAppType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}/action/cloneVAppTemplate:
    post:
      tags:
        - user
      description: Create a copy of a vApp template.
      responses:
        2XX:
          description: |-
            VAppTemplateType

             The response includes a Task element. You can monitor the task to to track the creation of the copy. TODO: Add @Supported in API version 39 to indicate removedIn value. JIRA: https://jira.eng.vmware.com/browse/COR-10202
          content:
            application/vnd.vmware.vcloud.vAppTemplate+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_VAppTemplateType'
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}/action/composeVApp:
    post:
      tags:
        - user
      description: Compose a vApp from existing virtual machines.
      responses:
        2XX:
          description: |-
            VAppType

             The response includes a Task element. You can monitor the task to to track the creation of the vApp.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/edgeGateway/{id}/action/configureServices:
    post:
      tags:
        - admin
      description: Update edgeGateway configuration.
      responses:
        2XX:
          description: TaskType
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/edgeGateway/{id}/action/configureSyslogServerSettings:
    post:
      tags:
        - admin
      description: Configure Syslog server settings for the org vdc edge gateway
      responses:
        2XX:
          description: TaskType
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/consolidate:
    post:
      tags:
        - user
      description: Consolidate VM snapshots.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/action/consolidate:
    post:
      tags:
        - user
      description: Consolidate VM snapshots.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /org/{id}/catalog/{catalogId}/action/controlAccess:
    post:
      tags:
        - user
      description: Update access control information for a catalog.
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.controlAccess+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_ControlAccessParamsType'
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      - in: path
        name: catalogId
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/controlAccess:
    post:
      tags:
        - user
      description: Update access control information for a vApp.
      responses:
        2XX:
          description: ControlAccessParamsType
          content:
            application/vnd.vmware.vcloud.controlAccess+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_ControlAccessParamsType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/convertStandAloneVmToVApp:
    post:
      tags:
        - user
      description: Convert a stand alone VM into a single VM VApp.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/edgeGateway/{id}/action/convertToAdvancedGateway:
    post:
      tags:
        - admin
      description: Mark a gateway as an advanced gateway.
      responses:
        2XX:
          description: TaskType
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/network/{id}/action/convertToDistributedInterface:
    post:
      tags:
        - admin
      description: |-
        Convert org vdc network to a distributed interface

         This operation is valid for internal org vdc routed networks that are subinterfaces or internal.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/network/{id}/action/convertToInternalInterface:
    post:
      tags:
        - admin
      description: |-
        Convert org vdc network to an interface

         This operation is valid for internal org vdc routed networks that are subinterfaces
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/network/{id}/action/convertToSubInterface:
    post:
      tags:
        - admin
      description: |-
        Convert org vdc network to an sub interface

         This operation is valid for internal org vdc routed networks
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/convertAllVAppVmsToStandAloneVms:
    post:
      tags:
        - user
      description: Convert single VM vApp to a stand alone VM.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/convertToReplicationTrackingVm:
    post:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /catalog/{id}/action/copy:
    post:
      tags:
        - user
      description: Copy a catalog item from one catalog to another.
      responses:
        2XX:
          description: |-
            TaskType

             The response includes a Task element. You can monitor the task to track the copy of the catalog item.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /catalog/{id}/catalogItems:
    post:
      tags:
        - user
      description: Add an item to a catalog.
      responses:
        2XX:
          description: CatalogItemType
          content:
            application/vnd.vmware.vcloud.catalogItem+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_CatalogItemType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}/disk:
    post:
      tags:
        - user
      description: Create a disk.
      responses:
        2XX:
          description: DiskType
          content:
            application/vnd.vmware.vcloud.disk+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_DiskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/networkPools:
    post:
      tags:
        - extension
      description: Create a network pool.
      responses:
        2XX:
          description: VMWNetworkPool
      deprecated: true
      security:
        - bearerAuth: []
  /admin/orgs:
    post:
      tags:
        - admin
      description: Create an organization.
      responses:
        2XX:
          description: AdminOrgType
          content:
            application/vnd.vmware.admin.organization+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_AdminOrgType'
      deprecated: true
      security:
        - bearerAuth: []
  /admin/extension/externalnets:
    post:
      tags:
        - extension
      description: Create an external network.
      responses:
        2XX:
          description: VMWExternalNetworkType
      deprecated: true
      security:
        - bearerAuth: []
  /admin/extension/providervdcsparams:
    post:
      tags:
        - extension
      description: Create a provider vDC.
      responses:
        2XX:
          description: |-
            VMWProviderVdcType

             This operation is asynchronous. The response includes a Task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
  /admin/org/{orgId}/roles:
    post:
      tags:
        - admin
      description: Creates a role or optionally creates a role template along with role instances based on the template for a sys-admin client at API version \< 27
      responses:
        2XX:
          description: RoleType
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: orgId
        required: true
        schema:
          type: string
        style: simple
  /admin/roles:
    post:
      tags:
        - admin
      description: Creates a role or optionally creates a role template along with role instances based on the template for a sys-admin client at API version \< 27
      responses:
        2XX:
          description: RoleType
      deprecated: true
      security:
        - bearerAuth: []
  /vApp/{id}/action/createSnapshot:
    post:
      tags:
        - user
      description: Creates new snapshot of a virtual machine or of all the virtual machines in a vApp. Prior to creation of the new snapshots, any existing user created snapshots associated with the virtual machines are removed.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vdcTemplates:
    post:
      tags:
        - extension
      description: Create a VDC template.
      responses:
        2XX:
          description: vdcTemplate the new VDC Template.
      security:
        - bearerAuth: []
  /admin/org/{id}/users:
    post:
      tags:
        - admin
      description: |-
        Create or import a user.

         Users can be created locally or imported from the organization's LDAP service.
      responses:
        2XX:
          description: UserType
          content:
            application/vnd.vmware.admin.user+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_UserType'
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/vdcsparams:
    post:
      tags:
        - admin
      description: Creates an organization vDC.
      responses:
        2XX:
          description: |-
            AdminVdcType

             This operation is asynchronous. The response includes a Task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}/action/createVm:
    post:
      tags:
        - user
      description: Create a standalone blank VM and nest it under a new auto-nature vApp.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.

             The response includes a Task element. You can monitor the task to track the creation of the vApp. The owner of the task is the new vApp entity.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/deploy:
    post:
      tags:
        - user
      description: |-
        Deploy a vApp or VM.

         Deployment allocates all resources for the vApp and the virtual machines it contains.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/disk/action/detach:
    post:
      tags:
        - user
      description: Detach a disk from a VM.
      parameters:
        - in: query
          name: force
          schema:
            $ref: '#/components/schemas/query-parameter_force'
          style: form
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/rule/{ruleId}/action/disable:
    post:
      tags:
        - extension
      description: Disables the specified VM Host affinity rule.
      responses:
        2XX:
          description: a task to monitor disabling of the rule
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: ruleId
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/datastore/{id}/action/disable:
    post:
      tags:
        - extension
      description: Disable a datastore.
      responses:
        2XX:
          description: Datastore
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/edgeGateway/{id}/action/disableDistributedRouting:
    post:
      tags:
        - admin
      description: Disable distributed routing for a gateway
      responses:
        2XX:
          description: TaskType
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/disableNestedHypervisor:
    post:
      tags:
        - user
      description: Hide hardware-assisted CPU virtualization from guest OS.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/action/disable:
    post:
      tags:
        - admin
      description: |-
        Disable an organization.

         Disabling an organization prevents users from logging in to it.
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/providervdc/{id}/action/disable:
    post:
      tags:
        - extension
      description: |-
        Disable a provider vDC.

         You can disable a provider vDC to prevent the creation of organization vDCs that use its resources. When you disable a provider vDC, you also disable the organization vDCs that use its resources. Running vApps and powered on virtual machines continue to run in those vDCs, but users cannot deploy additional vApps in them.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/resourcePool/{id}/action/disable:
    post:
      tags:
        - extension
      description: Disable a provider vDC resource pool.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/disableDownload:
    post:
      tags:
        - user
      description: Disable a vApp for download.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/action/disableDownload:
    post:
      tags:
        - user
      description: Disable a vApp template for download.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/vdc/{id}/action/disable:
    post:
      tags:
        - admin
      description: |-
        Disable an organization vDC.

         Disabling an organization vDC prevents prevent any further allocation of resources to it, but does not affect existing allocations. When an organization vDC is disabled, an organization user cannot deploy new virtual machines in it.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/discardSuspendedState:
    post:
      tags:
        - user
      description: |-
        Discard suspended state of a vApp or VM.

         Discarding the suspended state of a vApp discards the suspended state of all VMs it contains.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/media/action/ejectMedia:
    post:
      tags:
        - user
      description: Eject virtual media.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/rule/{ruleId}/action/enable:
    post:
      tags:
        - extension
      description: Enables the specified VM Host affinity rule.
      responses:
        2XX:
          description: a task to monitor enabling of the rule
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: ruleId
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/datastore/{id}/action/enable:
    post:
      tags:
        - extension
      description: Enable a datastore.
      responses:
        2XX:
          description: Datastore
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/edgeGateway/{id}/action/enableDistributedRouting:
    post:
      tags:
        - admin
      description: Enable distributed routing for a gateway
      responses:
        2XX:
          description: TaskType
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /media/{id}/action/enableDownload:
    post:
      tags:
        - user
      description: Enable download for the media
      responses:
        2XX:
          description: success
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/action/enable:
    post:
      tags:
        - admin
      description: |-
        Enable an organization.

         Enabling an organization allows users to log in to it.
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/providervdc/{id}/action/enable:
    post:
      tags:
        - extension
      description: Enable a provider vDC.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/resourcePool/{id}/action/enable:
    post:
      tags:
        - extension
      description: Enable a provider VDC resource pool
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/enableDownload:
    post:
      tags:
        - user
      description: Enable a vApp for download.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vAppTemplate/{id}/action/enableDownload:
    post:
      tags:
        - user
      description: Enable a vApp template for download
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/vdc/{id}/action/enable:
    post:
      tags:
        - admin
      description: Enable an organization vDC.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/enableNestedHypervisor:
    post:
      tags:
        - user
      description: Expose hardware-assisted CPU virtualization to guest OS.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/enterMaintenanceMode:
    post:
      tags:
        - user
      description: |-
        Place the vApp in maintenance mode.

         When a vApp is in maintenance mode, it is read-only to users. Only a system administrator can modify it. User-initiated tasks that are running when the vApp enters maintenance mode continue to run.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/exitMaintenanceMode:
    post:
      tags:
        - user
      description: Take the vApp out of maintenance mode.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/settings/vAppLeaseSettings/action/expireVAppsRuntimeLease:
    post:
      tags:
        - admin
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/settings/vAppTemplateLeaseSettings/action/expireVAppTemplatesStorageLease:
    post:
      tags:
        - admin
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/settings/vAppLeaseSettings/action/expireVAppsStorageLease:
    post:
      tags:
        - admin
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/blockingTask/{id}/action/fail:
    post:
      tags:
        - extension
      description: |-
        Fail a blocking task.

         This request ends the task, setting the status of any transient objects that it created to ERROR. Task status is set to ERROR.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/customizeAtNextPowerOn:
    post:
      tags:
        - user
      description: Force guest customization on next power on.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/strandedItem/{id}/action/forceDelete:
    post:
      tags:
        - extension
      description: Removes stranded item and its children
      responses:
        2XX:
          description: Task created to remove the stranded item
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vimServer/{id}/action/forcevimserverreconnect:
    post:
      tags:
        - extension
      description: Force vCloud Director to reconnect to a registered vCenter server.
      responses:
        2XX:
          description: TaskType This operation is asynchronous and the user should monitor the returned task status in order to check when it is completed.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/groups:
    post:
      tags:
        - admin
      description: Import a group from the LDAP directory service.
      responses:
        2XX:
          description: GroupType
          content:
            application/vnd.vmware.admin.group+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_GroupType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vimServer/{id}/importMedia:
    post:
      tags:
        - extension
      description: Import media from vCenter.
      responses:
        2XX:
          description: |-
            MediaType

             This operation is asynchronous. The response contains an embedded task object that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vimServer/{id}/importVm:
    post:
      tags:
        - extension
      description: Import a virtual machine from vCenter.
      responses:
        2XX:
          description: |-
            VmType

             This operation is asynchronous. The `status` of `VmType` can be used to monitor the import process.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vimServer/{id}/importVmAsVApp:
    post:
      tags:
        - extension
      description: Import a virtual machine from vCenter as a vCloud Director vApp.
      responses:
        2XX:
          description: |-
            VAppType

             This operation is asynchronous. The response contains an embedded task object that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.vApp+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_VAppType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vimServer/{id}/importVmAsVAppTemplate:
    post:
      tags:
        - extension
      description: Import a virtual machine from vCenter as a vCloud Director vApp template.
      responses:
        2XX:
          description: |-
            VAppTemplateType

             This operation is asynchronous. The response contains an embedded task object that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.vAppTemplate+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_VAppTemplateType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vimServer/{id}/importVmIntoExistingVApp:
    post:
      tags:
        - extension
      description: Import a virtual machine from vCenter into an existing vCloud Director vApp.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/media/action/insertMedia:
    post:
      tags:
        - user
      description: Insert virtual media.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/installVMwareTools:
    post:
      tags:
        - user
      description: Install VMware Tools on a running VM.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}/action/instantiateOvf:
    post:
      tags:
        - user
      description: Instantiate a vApp or VM from an OVF
      responses:
        2XX:
          description: |-
            VAppType

             The response includes an upload link for the OVF descriptor
          content:
            application/vnd.vmware.vcloud.vApp+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_VAppType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}/action/instantiateVAppTemplate:
    post:
      tags:
        - user
      description: Create a vApp from a vApp template
      responses:
        2XX:
          description: |-
            VAppType

             The response includes a Task element. You can monitor the task to to track the creation of the vApp.
          content:
            application/vnd.vmware.vcloud.vApp+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_VAppType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /org/{id}/action/instantiate:
    post:
      tags:
        - user
      description: Instantiates a VDC template. The caller supplies the VDC template and the name and description (as a ParamsType), which are the only two things the tenant can override in the template.
      responses:
        2XX:
          description: vdcTemplate
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}/action/instantiateVmFromTemplate:
    post:
      tags:
        - user
      description: Create a auto natured vApp from a VM template. This API will be internal and the intended client for it is H5 UI
      responses:
        2XX:
          description: '{@link TaskType} task to track the creation of the vApp.'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/rule/{ruleId}/action/makeMandatory:
    post:
      tags:
        - extension
      description: Make specified VM Host affinity rule mandatory.
      responses:
        2XX:
          description: a task to monitor enabling of the rule
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: ruleId
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/rule/{ruleId}/action/makeOptional:
    post:
      tags:
        - extension
      description: Make the specified VM Host affinity optional.
      responses:
        2XX:
          description: a task to monitor enabling of the rule
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: ruleId
        required: true
        schema:
          type: string
        style: simple
  /vdcStorageProfile/{id}/action/makeDefault:
    post:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/providervdc/{id}/action/merge:
    post:
      tags:
        - extension
      description: |-
        Merge provider vDCs.

         This operation merges the provider vDC specified in the request URL with one or more provider vDCs specified in the request parameters, and returns a representation of the merged provider vDC.
      responses:
        2XX:
          description: VMWProviderVdc
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/action/migrateTenant:
    post:
      tags:
        - extension
      description: Initiate relocation of all entities for a given org (tenant).
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
  /admin/extension/resourcePool/{id}/action/migrateVms:
    post:
      tags:
        - extension
      description: Migrate one or more virtual machines to a different resource pool.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task. When the task completes, the virtual machines on the list have been migrated.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/edgeGateway/{id}/action/modifyFormFactor:
    post:
      tags:
        - admin
      description: Modify edgeGateway form factor.
      responses:
        2XX:
          description: TaskType
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /catalog/{id}/action/move:
    post:
      tags:
        - user
      description: Move a catalog item from one catalog to another.
      responses:
        2XX:
          description: |-
            TaskType

             The response includes a Task element. You can monitor the task to track the move of the catalog item.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /disk/{id}/action/moveDisk:
    post:
      tags:
        - user
      description: Move a disk to a different Org VDC.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}/action/moveVApp:
    post:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/events:
    post:
      tags:
        - admin
      description: Post an external event to the system.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/power/action/powerOff:
    post:
      tags:
        - user
      description: |-
        Power off a vApp or VM.

         If used on a vApp, powers off all VMs in the vApp. If used on a VM, powers off the VM. This operation is available only for a vApp or VM that is powered on.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/power/action/powerOn:
    post:
      tags:
        - user
      description: |-
        Power on a vApp or VM.

         If used on a vApp, powers on all VMs in the vApp. If used on a VM, powers on the VM. This operation is available only for a vApp or VM that is powered off.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/failover:
    post:
      tags:
        - user
      description: Promote a replication tracking VM to a standard VM
      responses:
        2XX:
          description: Task tracking progress of the VM promotion
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/user/{id}/entityRights:
    post:
      tags:
        - admin
      description: Queries user privileges on a set of entities
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/reapplyComputePolicy:
    post:
      tags:
        - user
      description: Reapplies existing vdcComputePolicy to a VM
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/edgeGateway/{id}/action/reapplyServices:
    post:
      tags:
        - admin
      description: Reapply services on an edgeGateway.
      responses:
        2XX:
          description: TaskType
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/power/action/reboot:
    post:
      tags:
        - user
      description: |-
        Reboot a vApp or VM.

         If used on a vApp, reboots all VMs in the vApp. If used on a VM, reboots the VM. This operation is available only for a vApp or VM that is powered on.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/recomposeVApp:
    post:
      tags:
        - user
      description: Recompose a vApp to add or remove VMs.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/reconfigureVm:
    post:
      tags:
        - user
      description: "Updates Vm name, Description, and any or all of the following sections.  \n\n* VirtualHardwareSection\n* OperatingSystemSection\n* NetworkConnectionSection\n* GuestCustomizationSection\n\n Sections not included in the request body will not be updated. In API requests that specify version 29.0 or higher, this operation allows to also update:\n\n* the boot-options of the VM (boot-delay and enter-bios).\n* the storage profile\n* the VM capabilities (ability to add CPU or MEM while the VM is powered-on)\n* the hardware-assisted CPU virtualization capability"
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/edgeGateway/{id}/action/redeploy:
    post:
      tags:
        - admin
      description: Redeploy edgeGateway.
      responses:
        2XX:
          description: TaskType
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vimServer/{id}/action/refreshStorageProfiles:
    post:
      tags:
        - extension
      description: Refresh Storage Classes data from a registered vCenter server.
      responses:
        2XX:
          description: TaskType This operation is asynchronous and the user should monitor the returned task status in order to check when it is completed.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vimServer/{id}/action/refresh:
    post:
      tags:
        - extension
      description: Refresh vCenter server.
      responses:
        2XX:
          description: TaskType Asynchronous task representing refresh operation.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/settings/federation/action/regenerateFederationCertificate:
    post:
      tags:
        - admin
      description: Regenerates the certificates used to establish trust between an organization and its identity provider
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/associations/localAssociationData/action/regenerateKeyPair:
    post:
      tags:
        - admin
      description: Regenerates an org's key pair for associations
      responses:
        2XX:
          description: Task reference
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /site/associations/localAssociationData/action/regenerateKeyPair:
    post:
      tags:
        - user
      description: Add a new {@link SiteAssociationType} for this site. POST /api/site/action/localAssociationData/regenerateSiteKey
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
  /admin/vdc/{id}/action/registerVApp:
    post:
      tags:
        - admin
      description: Create a vApp based on a set of .vmx files and resource mappings
      responses:
        2XX:
          description: |-
            VAppType

             The response includes a Task element. You can monitor the task to to track the creation of the vApp.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/action/registervimserver:
    post:
      tags:
        - extension
      description: Register a vCenter server and vShield manager.
      responses:
        2XX:
          description: RegisterVimServerParamsType
      deprecated: true
      security:
        - bearerAuth: []
  /vApp/{id}/action/reloadFromVc:
    post:
      tags:
        - user
      description: |-
        Reloads VM state from VC. Checks VM's extra configuration properties to ensure that they include all the default properties required for cloud-managed VMs and do not include any denylisted properties or values. Reconfigures VM to add default properties and remove denylisted properties and values if needed.

         This API is available to sysadmin users only.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/removeAllSnapshots:
    post:
      tags:
        - user
      description: Removes all user created snapshots for a vApp or virtual machine.
      parameters:
        - in: query
          name: force
          schema:
            $ref: '#/components/schemas/query-parameter_force'
          style: form
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vmGroup/name/{groupName}/action/removeVms:
    post:
      tags:
        - extension
      description: |-
        Removes virtual machines from the specified VM group.

         This asynchronous call returns a task. The process of removing virtual machines from a VM group can result in the virtual machine being processed and redeployed by the placement engine. All virtual machines provided to this call will be marked busy until the remove from group process is complete.
      responses:
        2XX:
          description: a task to monitor the progress of removing virtual machines from a VM group
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: groupName
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/networkPool/{id}/action/repair:
    post:
      tags:
        - extension
      description: Repair a network pool. This command ensures that the defined scope of the network pool is reflected in the scope of its corresponding transport zone.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/network/{id}/action/reset:
    post:
      tags:
        - admin
      description: |-
        Reset networking services on a network.

         This operation is valid for all networks except vApp networks in undeployed vApps. Starting from API Version 35.0, this API is deprecated when using an Org vDC Network. Refer to OpenAPI.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/power/action/reset:
    post:
      tags:
        - user
      description: |-
        Reset a vApp or VM.

         If used on a vApp, resets all VMs in the vApp. If used on a VM, resets the VM. This operation is available only for a vApp or VM that is powered on.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/settings/general/action/resetVcTrustsore:
    post:
      tags:
        - extension
      description: Resets vCenter trust store.
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
  /admin/extension/blockingTask/{id}/action/resume:
    post:
      tags:
        - extension
      description: |-
        Resume a blocking task.

         This request unblocks the task and allows it to continue.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/settings/oauth/openIdProviderConfig:
    post:
      tags:
        - admin
      description: Reads an OpenID configuring from a well-known endpoint and responds with the raw information from the IDP as well as VCD's interpretation of that information. Note that this action does not alter the actual settings for the organization.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/revertToCurrentSnapshot:
    post:
      tags:
        - user
      description: Reverts a vApp or virtual machine to the current snapshot, if any.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/org/{id}/catalogs:
    post:
      tags:
        - admin
      description: |-
        Create a catalog.

         The catalog is created in the unpublished state.
      responses:
        2XX:
          description: |-
            CatalogType

             This operation is asynchronous. The response body includes a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.admin.catalog+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_AdminCatalogType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/catalog/{id}/action/subscribeToExternalCatalog:
    post:
      tags:
        - admin
      description: Subscribe to an external catalog.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/power/action/shutdown:
    post:
      tags:
        - user
      description: |-
        Shut down a vApp or VM.

         If used on a vApp, shuts down all VMs in the vApp. If used on a VM, shuts down the VM. This operation is available only for a vApp or VM that is powered on.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/power/action/suspend:
    post:
      tags:
        - user
      description: |-
        Suspend a vApp or VM.

         If used on a vApp, suspends all VMs in the vApp. If used on a VM, suspends the VM. This operation is available only for a vApp or VM that is powered on.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /catalog/{id}/action/sync:
    post:
      tags:
        - user
      description: Force sync the catalog to the remote subscribed catalog.
      responses:
        2XX:
          description: |-
            TaskType

             The response includes a Task element. You can monitor the task to to track the sync of the catalog.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /catalogItem/{id}/action/sync:
    post:
      tags:
        - user
      description: Synchronize a catalog item with its source object.
      responses:
        2XX:
          description: |-
            {@link TaskType}

             The response is a Task element. You can monitor the task to track the progress of this operation.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/networkPool/{id}/action/sync:
    post:
      tags:
        - extension
      description: Sync a network pool. The command updates the networking pool to match the defined scope of its corresponding transport zone.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/network/{id}/action/syncSyslogServerSettings:
    post:
      tags:
        - admin
      description: |-
        Synchronize syslog server settings for a network.

         When you change the IP addresses of the primary or secondary syslog server for a cloud, you must also synchronize the syslog server settings for organization and vApp networks that include a firewall service for which logging has been enabled. Starting from API Version 35.0, this API is deprecated when using an Org vDC Network. Refer to OpenAPI.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/edgeGateway/{id}/action/syncSyslogServerSettings:
    post:
      tags:
        - admin
      description: Synchronize syslog server settings on an edgeGateway.
      responses:
        2XX:
          description: TaskType
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/user/{id}/action/takeOwnership:
    post:
      tags:
        - admin
      description: Transfer ownership of this user's vApps, media, and catalogs to the caller.
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/settings/amqp/action/test:
    post:
      tags:
        - extension
      description: |-
        Validate provisional AMQP settings.

         This request attempts to contact an AMQP broker using the values in the request body (an AmqpSettings element). If the attempt succeeds, the AmqpSettings are considered valid and can be used to update the system's AMQP settings.
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
  /vApp/{id}/action/undeploy:
    post:
      tags:
        - user
      description: |-
        Undeploy a vApp/VM.

         Undeployment deallocates all resources used by the vApp and the VMs it contains.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/user/{id}/action/unlock:
    post:
      tags:
        - admin
      description: Unlock a user.
      responses:
        2XX:
          description: success
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vimServer/{id}/action/unregister:
    post:
      tags:
        - extension
      description: Unregister a vCenter server.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      deprecated: true
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/bootOptions:
    post:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/catalog/{id}/action/publish:
    post:
      tags:
        - admin
      description: |-
        Publish a catalog.

         Publishing a catalog sets the value of its IsPublished element to true and makes the catalog visible to organization administrators of all organizations in a cloud.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/catalog/{id}/action/publishToExternalOrganizations:
    post:
      tags:
        - admin
      description: Publish a catalog to external orgs.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/blockingTask/{id}/action/updateProgress:
    post:
      tags:
        - extension
      description: |-
        Extend the expiration timeout of an active task.

         This request resets the timeout value and timeout action for an active task. Use this request to keep the task alive when it might become subject to a timeout action.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/providervdc/{id}/storageProfiles:
    post:
      tags:
        - extension
      description: |-
        Update storage classes of a provider vDC.

         The request parameters specify storage classes to add or remove.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/providervdc/{id}/action/updateResourcePools:
    post:
      tags:
        - extension
      description: Add or remove provider vDC resource pools.
      responses:
        2XX:
          description: |-
            {@link TaskType}

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/editTrustedPlatformModule:
    post:
      tags:
        - user
      description: ""
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/vdc/{id}/vdcStorageProfiles:
    post:
      tags:
        - admin
      description: Add or remove virtual datacenter storage classes.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/edgeGateway/{id}/action/upgradeConfig:
    post:
      tags:
        - admin
      description: Upgrades the edge gateway appliance size.
      responses:
        2XX:
          description: TaskType
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vApp/{id}/action/upgradeHardwareVersion:
    post:
      tags:
        - user
      description: Upgrade the virtual hardware version of a VM to the highest supported virtual hardware version of the vDC that contains it.
      responses:
        2XX:
          description: |-
            TaskType

             This operation is asynchronous and returns a task that you can monitor to track the progress of the request.
          content:
            application/vnd.vmware.vcloud.task+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_TaskType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/settings/amqp/action/updateAmqpCertificate:
    post:
      tags:
        - extension
      description: Updates AMQP certificate. AMQP certificate and trust store are mutually exclusive. Overrides AMQP trust store, if update is successful.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
  /admin/extension/settings/amqp/action/updateAmqpTruststore:
    post:
      tags:
        - extension
      description: Updates AMQP trust store. AMQP certificate and trust store are mutually exclusive. Overrides AMQP certificate, if update is successful.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      security:
        - bearerAuth: []
  /admin/extension/service/localizationbundles:
    post:
      tags:
        - extension
      description: Initiates localization bundle upload.
      responses:
        2XX:
          description: BundleUploadSocketType containing URL where bundle must be uploaded.
      security:
        - bearerAuth: []
  /catalog/{id}/action/upload:
    post:
      tags:
        - user
      description: Upload a vApp template to a catalog
      responses:
        2XX:
          description: |-
            CatalogItemType

             The response is the CatalogItemType. The API client then need to make another call to the referenced media entity in order to get the upload URI.
          content:
            application/vnd.vmware.vcloud.catalogItem+json;version=39.0:
              schema:
                $ref: '#/components/schemas/vcloud_CatalogItemType'
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/settings/general/action/updateVcTrustsore:
    post:
      tags:
        - extension
      description: Updates vCenter trust store.
      responses:
        2XX:
          description: '{@link JAXBElement}'
      deprecated: true
      security:
        - bearerAuth: []
  /admin/vdcTemplate/{id}/action/controlAccess:
    put:
      tags:
        - admin
      description: Update access control information for a VDC template.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/extension/vdcTemplate/{id}/action/controlAccess:
    put:
      tags:
        - extension
      description: Update access control information for a VDC template.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /vdc/{id}/action/controlAccess:
    put:
      tags:
        - user
      description: Update access control information for a VDC. Provided access control information should only use ReadOnly access level or else {@link BadRequestException} is thrown
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
  /admin/edgeGateway/{id}/action/updateProperties:
    put:
      tags:
        - admin
      description: Update just the properties of an edge gateway (such as name, HA, IP Allocations, etc). Can only be used for Edge Gateways owned by an Organization vDC. API Version 35.0 is the last version this API is supported for NSX-T Edges.
      responses:
        2XX:
          description: success
      security:
        - bearerAuth: []
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
components:
  securitySchemes:
    basicAuth:
      type: http
      scheme: basic
    bearerAuth:
      type: http
      scheme: bearer
  schemas:
    query-parameter_force:
      type: boolean
    query-parameter_recursive:
      type: boolean
    query-parameter_fields:
      type: string
    query-parameter_filter:
      type: string
    query-parameter_filterEncoded:
      type: boolean
    query-parameter_format:
      type: string
      enum:
        - references
        - records
        - idrecords
    query-parameter_links:
      type: boolean
    query-parameter_offset:
      type: integer
      minimum: 0
    query-parameter_page:
      type: integer
      minimum: 1
    query-parameter_pageSize:
      type: integer
      minimum: 1
      maximum: 128
    query-parameter_sortAsc:
      type: string
    query-parameter_sortDesc:
      type: string
    query-parameter_type:
      type: string
      enum:
        - aclRule
        - adminApiDefinition
        - adminCatalog
        - adminCatalogItem
        - adminDisk
        - adminEvent
        - adminFileDescriptor
        - adminGroup
        - adminMedia
        - adminOrgVdc
        - adminOrgVdcStorageProfile
        - adminOrgVdcTemplate
        - adminRole
        - adminService
        - adminShadowVM
        - adminTask
        - adminUser
        - adminVApp
        - adminVAppNetwork
        - adminVAppTemplate
        - adminVM
        - adminVMDiskRelation
        - apiFilter
        - blockingTask
        - catalog
        - catalogItem
        - cell
        - condition
        - datastore
        - datastoreProviderVdcRelation
        - dvSwitch
        - edgeGateway
        - externalLocalization
        - externalNetwork
        - gatewayUplinks
        - host
        - networkPool
        - nsxTManager
        - orgAssociation
        - orgNetwork
        - orgVdc
        - orgVdcNetwork
        - orgVdcResourcePoolRelation
        - orgVdcStorageProfile
        - orgVdcTemplate
        - organization
        - portgroup
        - providerVdc
        - providerVdcResourcePoolRelation
        - providerVdcStorageProfile
        - resourceClass
        - resourceClassAction
        - resourcePool
        - resourcePoolVmList
        - right
        - role
        - serviceLink
        - serviceResource
        - site
        - siteAssociation
        - siteOrgAssociation
        - storageProfile
        - strandedItem
        - strandedUser
        - vApp
        - vAppNetwork
        - vAppOrgVdcNetworkRelation
        - vAppTemplate
        - virtualCenter
        - vm
        - vmDiskRelation
        - vmGroupVms
        - vmGroups
tags:
  - name: user
    description: These operations and queries are accessible to all users who have permission to log into an organization. User elements and user types represent the objects that these operations manipulate.
  - name: admin
    description: These operations and queries are accessible to organization administrators or system administrators. Administrator elements and types represent the objects that these operations manipulate.
  - name: extension
    description: These operations and queries are accessible to system administrators who need access to vSphere platform objects from the VMware Cloud Director API. Extension elements and types represent the objects that these operations manipulate.
